# DVARO — Module Status

## Infrastructure
- [Done] Project scaffold + 14-module structure
- [Done] Multi-tenancy foundation (TenantScope + TenantMiddleware)
- [In Progress] Auth guards
  - [Done] tenant guard (login/logout) — see SaaS Core below
  - [Done] mechanic guard (4th guard; PIN or password login) — see Workshop & Mechanic Portal below
  - [Done] superadmin guard (platform-wide, NOT tenant-scoped) — see Super Admin Panel below
  - [Done] customer guard (5th guard; CustomerUser model, tenant-scoped login) — see Customer Portal below
- [In Progress] Provider interfaces — SMS/Email/WhatsApp/AI implemented (Notification/AI sessions); Payment: Stripe implemented (Session 26), PayPal pending
- [ ] CI/CD pipeline (GitHub Actions)

## Modules
- [In Progress] SaaS Core (plans, subscriptions, tenant onboarding)
  - [Done] Plan model + migration (platform-wide, no tenant scope; cents; `getLimit()` returns -1 = unlimited)
  - [Done] Subscription model + migration (HasTenant; status/billing constants; active/trialing/onTrial scopes)
  - [Done] PlanEnforcementService (hard block; limit < 0 = unlimited, skipped)
  - [Done] PlanLimitExceededException (403 JSON, code PLAN_LIMIT_REACHED)
  - [Done] TenantOnboardingService + DTO + TenantRegistered event (DB::transaction, trialing subscription)
  - [Done] Tenant model: activeSubscription/activePlan/hasModule/withinLimit; plan_id FK (nullOnDelete)
  - [Done] Tenant Portal Authentication (tenant guard — login/logout only; NO password reset, NO registration UI):
    - tenant_users migration (HasTenant; UNIQUE(tenant_id, email) — email is per-tenant, NOT global; role string; rememberToken)
    - TenantUser model — DEDICATED, separate from App\Models\User; extends Authenticatable; HasTenant + Spatie HasRoles + Notifiable; $guard_name='tenant' (so Spatie resolves roles under the tenant guard); password hashed cast; ROLE_ADMIN/STAFF/ACCOUNTS constants
    - config/auth.php: 'tenant_users' provider → TenantUser (other 3 portals still scaffold to User)
    - TenantRolesSeeder — creates tenant_admin/tenant_staff/tenant_accounts under guard 'tenant'; NOT wired into DatabaseSeeder, run on demand
    - TenantAuthController (showLogin/login/logout) + LoginRequest (email|ip rate limit, 5 attempts); login scoped to current_tenant — attempt() passes explicit tenant_id ON TOP of the automatic TenantScope on the provider lookup, so one tenant's credentials can never authenticate against another
    - routes/tenant.php: GET/POST login, POST logout + GET dashboard placeholder behind auth:tenant
    - Inertia shares auth.user (slim payload) lazily from the tenant guard; Vue Pages/Tenant/Login.vue (PublicLayout) + Dashboard.vue (AppLayout) — FUNCTIONAL ONLY, design pass later; added '@' → resources/js Vite alias
    - CRITICAL FIX: pinned TenantMiddleware into the middleware PRIORITY list before the AuthenticatesRequests contract (bootstrap/app.php prependToPriorityList). Laravel's priority sort otherwise hoists auth:tenant ahead of the tenant resolver, so auth would run before the tenant is bound. Anchored to the contract, not concrete Authenticate, because the default priority list references the interface.
    - redirectGuestsTo: unauthenticated tenant routes → that tenant's login (detected via bound current_tenant, not path re-parsing); falls back to '/' when no tenant bound
  - [Verified] Pest/PHPUnit feature suite tests/Feature/TenantAuthTest.php (DatabaseTransactions — NOT RefreshDatabase, never migrate:fresh): same email in 2 tenants = 2 distinct users; login to own tenant; tenant A creds REJECTED at tenant B login; session persists (re-resolved from session store via forgetGuards between requests); tenant A session does NOT authenticate on tenant B (redirects to B login); logout; guest→login redirect; rate limit on 6th attempt. 8 tests / full suite green.
  - [Done] Public tenant self-registration (pre-tenant signup; NO billing/plan-selection UI yet):
    - TenantRegistrationRequest — company_name/email/password(min 8, confirmed)/plan_id(nullable, exists); IP-only throttle 3/hour (ensureIsNotRateLimited + hitRateLimiter, hits every attempt so spam signups count too)
    - TenantOnboardingService EXTENDED: now also creates the first admin TenantUser (role tenant_admin + Spatie assignRole) INSIDE the same DB::transaction as Tenant + Subscription — user/role failure rolls the whole onboarding back. tenant_id set explicitly (no bound tenant during onboarding; INSERT skips TenantScope). Requires tenant_admin role under guard 'tenant' (TenantRolesSeeder). execute() return type unchanged (Tenant). company_name doubles as first admin's display name (no user-name field at signup yet)
    - TenantOnboardingDTO::fromRequest now maps form 'company_name' → tenant `name`
    - TenantRegistrationController: showRegister + register. register() order is LOAD-BEARING — (1) service->execute, (2) bind current_tenant FIRST, (3) scoped TenantUser query AFTER bind, (4) Auth::guard('tenant')->login; any other 2/3 order throws TenantNotResolvedException. try/catch → report() + friendly back()->withErrors (never leaks stack trace; txn already rolled back). session regenerate + redirect to tenant.dashboard
    - routes/web.php: GET/POST /register (web group, NO tenant middleware, NOT under app/{slug}); guarded by 'guest.tenant'
    - guest.tenant = RedirectIfTenantAuthenticated (app/Http/Middleware): "guest only" for pre-tenant routes by tenant-guard SESSION-KEY presence ONLY (no model resolution) → can't use stock guest:tenant which would trip TenantScope on these unbound routes; redirects to '/' when a tenant session exists. Alias registered in bootstrap/app.php
    - Vue Pages/Tenant/Register.vue (PublicLayout, useForm, posts /register) — FUNCTIONAL ONLY; i18n keys added to en.json (auth.register/company_name/confirm_password/create_account) + lang/en/common.php (register.failed/throttle)
  - [Done] Tenant Dashboard (functional):
    - TenantDashboardController@index — resolves current_tenant, reads activeSubscription.plan, computes trialDaysRemaining (only while trialing + not lapsed); props planName/subscriptionStatus/trialDaysRemaining (tenant name from shared 'tenant' prop). Replaced the placeholder closure route in routes/tenant.php (still behind auth:tenant)
    - Pages/Tenant/Dashboard.vue enhanced (AppLayout) to show tenant name, plan, subscription status, trial days; i18n keys dashboard.plan/subscription_status/trial_days_remaining/no_plan
  - [Verified] tests/Feature/TenantRegistrationTest.php (DatabaseTransactions; seeds TenantRolesSeeder): tenant+admin user+trialing subscription created together; auto-login on tenant guard; failed onboarding (no active plan) rolls everything back + guest + no tenant row; password confirmation enforced; /register redirects to '/' when already tenant-authenticated (and does NOT throw TenantNotResolved). 5 tests; full suite 15 green; npm build green.
  - [Done] Tenant password reset (Session 21 — tenant-scoped broker; reset link path embeds {tenant_slug}; global /find-workspace email→tenant resolver) — see Session 21 below
  - [ ] Email verification, plan-selection UI
  - [In Progress] Stripe/PayPal subscription billing — Stripe Checkout DONE (Session 26: hosted checkout, webhook activation, cancellation); PayPal pending
  - [ ] Super admin plan management UI
- [In Progress] Super Admin Panel (Fleetora platform owner dashboard — separate guard, route group, layout)
  - [Done] Data layer (2 migrations, plain `php artisan migrate` — never fresh):
    - super_admins (NO tenant_id — platform-wide; email GLOBALLY unique, unlike tenant_users/mechanics; password; role string; is_active default true; last_login_at; rememberToken; softDeletes)
    - platform_settings (key UNIQUE / value text nullable / type string|boolean|integer|json / description; one row per setting; migration SEEDS 9 defaults: platform_name, support_email, manual_tenant_approval, free_trial_enabled, free_trial_days=14, freemium_enabled, default_plan_id [null], max_tenants [null], maintenance_mode)
  - [Done] Models: SuperAdmin (extends Authenticatable; deliberately NO HasTenant — NEVER TenantScope; Spatie HasRoles guard_name='superadmin' + Notifiable + SoftDeletes; password hashed + is_active boolean casts; 4 ROLE_* consts + ROLES; scopeActive) · PlatformSetting (key/value/type/description; TYPE_* consts)
  - [Done] config/auth.php: 'super_admins' provider repointed App\Models\User → SuperAdmin. superadmin is the THIRD dedicated guard model (after tenant, mechanic); customer still scaffolds to User. Fully isolated — never mixed with any other guard
  - [Done] PlatformSettingsService — CACHE-FIRST get() (Redis-backed Cache::remember, prefix 'platform_settings:', 24h TTL; "__missing__" sentinel so a genuine miss isn't re-queried every call); casts value by the row's `type`; set() writes DB + Cache::forget(key) (busts that one key); all() for the settings screen. Never hits DB on a warm read
  - [Done] PlanService — create/update/toggle/delete for plans (create derives a UNIQUE slug from name; update re-slugs only on name change ignoring own id; delete() HARD-BLOCKS when subscriptions exist [RuntimeException] — though no destroy route is wired regardless; modules array_values'd, limits passed through)
  - [Done] Events TenantSuspended / TenantActivated (hold Tenant; no listeners yet — suspension/activation notices are a later session; dispatched directly from the controller, not registered in EventServiceProvider since they have no listeners)
  - [Done] SuperAdminPolicy — role gates platformOwner / billingAccess / supportAccess / contentAccess (platform_owner implicitly passes all; sourced from Spatie roles under guard 'superadmin'). Registered as Gate::define delegations in AppServiceProvider (NOT Gate::policy — these are model-less role checks). Controllers authorize via Gate::forUser(auth('superadmin')->user())->authorize('<ability>') — same load-bearing forUser pattern as every other module
  - [Done] Middleware (bootstrap/app.php aliases + wiring):
    - SuperAdminMiddleware (alias 'superadmin.auth') — verifies auth('superadmin')->check() + is_active (deactivated → logout+redirect), binds 'current_super_admin', shares slim auth.superAdmin Inertia payload. No tenant to resolve (platform-wide)
    - CheckImpersonation (alias 'check.impersonation') — added to the tenant route GROUP (['web','tenant','check.impersonation']); shares an 'impersonating' Inertia prop (active flag) ONLY when the session carries impersonator_superadmin_id. The tenant banner reads this shared prop exclusively (no API/localStorage), consistent with how 'tenant'/'auth' are shared
  - [Done] Requests: SuperAdminLoginRequest (email|ip rate limit 5/15min, 'superadmin|' key prefix; no tenant scoping) · StorePlanRequest (prices as CENTS; modules.* in Plan::MODULE_KEYS; limits.* nullable int min:-1; prepareForValidation drops unknown limit keys) + UpdatePlanRequest (same rules) · SystemSettingsRequest (platform_name/support_email required; default_plan_id nullable exists:plans; booleans + free_trial_days/max_tenants)
  - [Done] Plan model gained MODULE_KEYS (12 module slugs mirroring app/Modules) + LIMIT_KEYS (max_vehicles/max_staff_users/max_customers/max_storage_gb/max_file_size_mb) consts — the form checklist + validation source of truth
  - [Done] Tenant model gained subscriptions() + users() hasMany relations, BOTH with withoutGlobalScope(TenantScope) so they resolve in the unbound super admin context (no current_tenant)
  - [Done] Controllers (thin; logic in services; every action Gate::forUser(auth('superadmin')->user())): SuperAdminAuthController (showLogin/login [active-only attempt, stamps last_login_at]/logout) · SuperAdminDashboardController (platform KPIs — total/active/trial tenants, new-this-month, active recurring revenue [MRR-style: sum of each active/trialing sub's plan price for its cycle — NOT historical paid revenue, no platform payment ledger yet], recent signups; tenant-scoped Subscription queried withoutGlobalScope) · TenantManagementController (index ?status+?search incl. admin email via whereHas('users'), paginate 20; show counts users/vehicles/invoices [all scope-free + explicit tenant_id] + subscription history; suspend/activate [+ events, no-op when already in state]; impersonate [SESSION-ONLY: logs into tenant guard as the oldest tenant_admin TenantUser + stamps impersonator_superadmin_id; NO DB write]; stopImpersonating [clears BOTH: Auth::guard('tenant')->logout() AND session forget marker — in one method]) · PlanManagementController (index withCount subscriptions / create / store / edit / update / toggle; NO show, NO destroy; coerceBooleans maps unchecked checkboxes) · SubscriptionManagementController (index ?status+?plan_id paginate 20; show — both withoutGlobalScope; show takes string id NOT a typed model so implicit binding can't trip TenantScope; payments [] placeholder — no platform billing yet) · SystemSettingsController (show/update via PlatformSettingsService; platformOwner-gated)
  - [Done] routes/superadmin.php (group mounted at /superadmin, name superadmin., guard separate): login GET/POST + logout; behind 'superadmin.auth' → dashboard, tenants index/show/suspend/activate/impersonate, stop-impersonating, Route::resource('plans')->except(show,destroy) + plans/{plan}/toggle, subscriptions index/show, settings show/update. 20 routes. {tenant} binds by slug (Tenant not scoped → safe); {plan} not scoped → safe
  - [Done] Vue (FUNCTIONAL ONLY — design pass later; all strings i18n superadmin.*): SuperAdminLayout REBUILT (own sidebar nav: dashboard/tenants/plans/subscriptions/settings + header logout + mobile inline nav — never shares AppLayout). Pages/SuperAdmin/{Login (PublicLayout), Dashboard (5 KPI cards incl. recurring revenue via useCurrency + recent-signups table), Tenants/{Index (status tabs + counts + search + suspend/activate/impersonate actions), Show (counts + subscription history + lifecycle buttons)}, Plans/{Index (cards w/ pricing/modules/active toggle), Create, Edit} sharing Components/PlanForm.vue (AUD↔cents in the page transform; module checklist; per-limit inputs, blanks dropped = unlimited), Subscriptions/{Index (status + plan filters), Show}, Settings/Index (general / signups+trials / limits+maintenance)}. AppLayout gained the amber impersonation banner (reads shared 'impersonating' prop; "Stop impersonating" POSTs /superadmin/stop-impersonating). i18n: superadmin.* added to locales/en.json; common.superadmin.* flash strings in lang/en/common.php
  - [Done] SuperAdminRolesSeeder (4 roles guard 'superadmin' + default platform_owner from SUPER_ADMIN_EMAIL/PASSWORD/NAME env; idempotent firstOrCreate; warns + skips account when env unset; NOT wired into DatabaseSeeder — run on demand). .env.example gained SUPER_ADMIN_EMAIL/PASSWORD/NAME
  - [Verified] Bootstrapped tinker smoke (20 checks): owner seeded/active/has Spatie role/password hashed+verifies/no tenant_id attr; owner passes platformOwner+supportAccess gates; billing_manager passes billingAccess but DENIED platformOwner+supportAccess; settings defaults cast correctly (string/bool/int) + missing key returns default + set busts cache + bool round-trips; PlanService create derives slug, stores modules/limits, duplicate name → smoke-plan-2, toggle flips is_active
  - [Verified] tests/Feature/SuperAdminTest.php (DatabaseTransactions — never fresh; real HTTP stack): super admin login → dashboard + last_login_at stamped; inactive super admin rejected; super admin CANNOT reach tenant routes (→ tenant login, wrong guard); tenant user CANNOT reach superadmin routes (→ superadmin login, wrong guard); suspend → status + TenantSuspended + suspended tenant app 403s, activate → restored + TenantActivated; impersonate → redirect to tenant dashboard + tenant guard = the admin + session marker, stop → BOTH cleared (tenant guard null + marker missing); settings update persists + recaches. 7 tests / full Pest suite 32 green (113 assertions); npm build green; 20 superadmin routes registered
  - [ ] Tenant manual-approval enforcement on signup (setting exists, not yet read by onboarding); maintenance_mode + default-plan-on-breach wiring; super admin role MANAGEMENT UI (create/manage other super admins — accounts created via seeder/tinker today); platform revenue analytics; activity logs; CMS; demo/backup/notification-template management; super admin password reset (DECISION Session 21: manual/support-only by design — NOT a self-service flow, unlike the 3 tenant-scoped portal guards)
- [In Progress] CRM (leads, intake forms)
  - [Done] Lead foundation + intake/conversion workflow (NO SMS/email sending yet — link is shown to admin to copy/send manually; that's the Notification session):
    - leads migration (HasTenant via tenantId() macro; name/email/phone NOT NULL; everything the customer fills is nullable; status string default 'new'; token UNIQUE (Str::uuid()); token_expires_at nullable (NULL = never auto-expires) + expires_manually bool; submitted_at/converted_at; converted_customer_id → customers nullOnDelete (keep lead history); created_by → tenant_users nullOnDelete; softDeletes; index (tenant_id, status))
    - Lead model: 5 STATUS_* constants + STATUSES; date/datetime/bool casts; convertedCustomer()/creator() belongsTo; isExpired() (manual OR token_expires_at past); isConvertible() (new/contacted + not expired + submitted_at set — never convert an un-filled lead); pending()/converted() scopes; SoftDeletes
    - GenerateLeadLinkAction — URL::signedRoute('crm.intake.show', tenant_slug+token). PERMANENT signature (link lifetime is a lead-level concept via isExpired(), NOT URL expiry); slug read from $lead->tenant->slug so it's correct with no tenant bound
    - CreateLeadAction — generates UUID token (server-side, unguessable) + created_by from auth('tenant')->id(); creates lead; fires LeadCreated; returns ['lead','link']
    - ConvertLeadAction — guards isConvertible() else LeadNotConvertibleException; DB::transaction; REUSES CreateCustomerAction (single customer-creation path → max_customers hard block + CustomerCreated come free); maps lead→CreateCustomerDTO coercing NULL licence_number/emergency_* to '' (customers cols are NOT NULL; UI must treat '' as "not provided"); notes→risk_notes, address→address; rental_start_date/duration intentionally dropped (belong to future Agreement); updates lead converted; fires LeadConverted(lead, customer)
    - ExpireLeadAction — sets expires_manually=true; fires LeadExpired; idempotent
    - Events LeadCreated/LeadSubmitted/LeadConverted/LeadExpired (no listeners yet — Notification/audit sessions)
    - LeadNotConvertibleException (app/Exceptions) — renders 422 JSON {success:false, message, code:'LEAD_NOT_CONVERTIBLE'}
    - LeadPolicy (viewAny/create + view/update/delete/convert/expire same-tenant). Registered via Gate::policy in AppServiceProvider
    - IntakeFormController — PUBLIC (no auth, NO TenantMiddleware → current_tenant NOT bound). CRITICAL: resolves Tenant by {tenant_slug} then queries Lead with withoutGlobalScope(TenantScope) + explicit tenant_id (a bare Lead::query() would THROW TenantNotResolved here). Gated by signed URL (tamper→403), missing lead→404, isExpired→410. show() passes a pre-signed crm.intake.submit URL so the POST's signature check passes; submit() updates lead + submitted_at + fires LeadSubmitted, renders CRM/IntakeSuccess
    - LeadController (thin; Gate::forUser(auth('tenant')->user()) on EVERY action — same load-bearing pattern as Fleet/Customer): index (?status= validated vs STATUSES + per-status tab counts in one grouped query + ?search= name/phone + paginate 15->withQueryString), create, store (re-renders CRM/Lead/Create with generatedLink for admin to copy — no PRG since there's a one-time artifact to show), show (lead + regenerated link + isConvertible/isExpired props), destroy (soft), convert → ConvertLeadAction (redirect to new customer show), expire → ExpireLeadAction (back)
    - StoreLeadRequest (name/email/phone required, rest nullable, token_expires_at nullable|date|after:now); IntakeFormRequest (name/phone required, email nullable — walk-ins may have none, rest nullable). Submit throttled per TOKEN (not IP — NAT/shared office) 5/hour via named limiter 'crm-intake' registered in AppServiceProvider + throttle:crm-intake on the route
    - routes/web.php: PUBLIC GET/POST /intake/{tenant_slug}/{token} (crm.intake.show/submit), NO tenant/auth middleware. routes/tenant.php: Route::resource('leads')->except(edit,update) ({lead} binds via TenantScope → cross-tenant 404) + POST leads/{lead}/convert + /expire, under existing auth:tenant. 9 routes total
    - Vue Pages/CRM/Lead/{Index,Create,Show}.vue (AppLayout, i18n crm.*): Index = status tabs w/ counts + name/phone search + submitted indicator + convert/expire/delete actions; Create = form + reveals signed link w/ copy button after create; Show = full detail + link + convert (disabled unless server-computed isConvertible) + expire. Pages/CRM/{IntakeForm,IntakeSuccess}.vue = PublicLayout, English-only (no i18n per spec), mobile-responsive, posts to the pre-signed submitUrl. FUNCTIONAL ONLY — design pass later. i18n: crm.* in en.json + common.crm.* flash strings in lang/en/common.php
  - [Verified] Tinker smoke (22 checks): LeadCreated fires + UUID token + valid signed URL; not-convertible/throws before submit; convertible after submit; convert creates Customer + sets lead converted/converted_at/converted_customer_id + fires LeadConverted; notes→risk_notes + address carried + null licence→''; double-convert throws (422); manual expire → isExpired true + LeadExpired fired; past token_expires_at → expired; tenant B cannot see tenant A's lead (scoped) while scope-free+explicit tenant_id lookup (the public path) does. Full Pest suite 15 green; npm build green; 9 routes registered
  - [ ] Lead notification dispatch (send link via SMS/email/WhatsApp), lead rejection action, edit, secure-link submission timestamps UI (later/Notification sessions)
- [In Progress] Fleet Management
  - [Done] Vehicle model + migration (HasTenant; 6 status constants + STATUSES array; daily_rate in cents; registration_number unique per tenant (tenant_id+registration_number); qr_code_token nullable+globally unique, generation deferred; date casts on expiry/service fields; available/rented/needsService scopes; isAvailable / insuranceExpiringSoon / registrationExpiringSoon helpers; agreements() hasMany)
  - [Done] agreements.vehicle_id FK attached → vehicles.id (restrictOnDelete — vehicle can't be deleted with agreement history); Agreement->vehicle() belongsTo added
  - [Done] VehicleStatusChanged event (vehicle + old/new status; no listeners yet)
  - [Done] VehicleChanged event (agreement + old/new vehicle + changeDate; no listeners yet) — CLAUDE.md-required event that did not previously exist; created in the Invoice Engine session, fired only from VehicleChangeService::execute()
  - [Done] ChangeVehicleStatusAction — the ONLY sanctioned path to mutate vehicle status (validates against STATUSES, fires VehicleStatusChanged, no-op on unchanged status); never mutate status directly elsewhere
  - [Done] Fleet UI — controllers/routes/Vue (CRUD, no new models):
    - SoftDeletes: migration add_soft_deletes_to_vehicles_table + SoftDeletes trait on Vehicle. NO hard deletes — destroy() soft-deletes only. Soft delete is an UPDATE (sets deleted_at), so it never trips the agreements.vehicle_id restrictOnDelete FK; a vehicle with agreement history can still be archived. Trashed rows auto-excluded from route-model binding (→404) and the index list
    - VehicleCreated event (fired once from CreateVehicleAction; no listeners yet)
    - CreateVehicleDTO + UpdateVehicleDTO (fromRequest + toAttributes; daily_rate is CENTS per spec; no qr_code_token). UpdateVehicleDTO deliberately has NO status field
    - CreateVehicleAction — PlanEnforcementService::check('max_vehicles', Vehicle::count()) BEFORE insert (hard block → PlanLimitExceededException 403), then create + fire VehicleCreated. Count is tenant-scoped & excludes soft-deleted (archived ≠ a plan slot)
    - UpdateVehicleAction — thin $vehicle->update(); cannot touch status (DTO carries none), so ChangeVehicleStatusAction stays the only status path
    - StoreVehicleRequest — registration_number unique scoped EXPLICITLY to tenant_id (the unique rule runs raw query-builder SQL that bypasses TenantScope). UpdateVehicleRequest extends it: adds ->ignore($vehicle->id) and drops `status`. ChangeStatusRequest: status required, in Vehicle::STATUSES
    - VehiclePolicy (viewAny/create/view/update/delete/changeStatus; same-tenant check). Registered via Gate::policy in AppServiceProvider (module paths are NOT auto-discovered)
    - CRITICAL: controller authorizes via Gate::forUser(auth('tenant')->user())->authorize(...) on EVERY vehicle action — NOT $this->authorize() (which resolves the default web guard, empty here, silently skipping the tenant-user check). A loud comment block in FleetController enforces this for future devs. (No global gate() helper exists in Laravel — Gate facade used; forUser(tenant guard) is the load-bearing part)
    - FleetController (thin): index (?status= filter validated vs STATUSES + per-status tab counts + paginate(15)->withQueryString()), create/store, show, edit/update, destroy (soft), changeStatus → ChangeVehicleStatusAction. Flash success via __('common.fleet.*')
    - routes/tenant.php: Route::resource('fleet')->parameter('fleet','vehicle') + POST fleet/{vehicle}/status (name tenant.fleet.*), all under existing auth:tenant. HandleInertiaRequests now shares a `flash` prop (success/error)
    - Vue Pages/Fleet/{Index,Create,Edit,Show}.vue (AppLayout, useForm, all strings via i18n fleet.* + common.*) + Components/StatusBadge.vue (color map available=green/rented=blue/maintenance=yellow/suspended=gray/accident=red/reserved=purple). Edit & Show change status via the separate endpoint, never the edit form. FUNCTIONAL ONLY — design pass later
  - [Verified] Tinker smoke: VehicleCreated fires on create; max_vehicles=1 hard-blocks 2nd create; status change via action fires VehicleStatusChanged; soft delete hides row but keeps it with deleted_at set; tenant B cannot see tenant A's vehicle even withTrashed. Full Pest suite 15 green; npm build green; 8 fleet routes registered with {vehicle} binding
  - [ ] QR code token generation/resolution (Workshop session)
  - [ ] Utilisation/reporting, vehicle docs & image storage, expiry reminder notifications (later sessions)
- [In Progress] Agreement Engine
  - [Done] Agreement model + migration (HasTenant; type/status/billing constants; rate & bond_amount in cents; version + parent_agreement_id self-FK for immutable versioning; customer_id & parent_agreement_id restrictOnDelete; isSigned/isActive/latestVersion helpers; indexes on tenant_id+status and customer_id)
  - [Done] vehicle_id FK → vehicles.id (restrictOnDelete) attached in Fleet session; vehicle() belongsTo added
  - [Done] Agreement Engine — creation / signature / PDF / immutable versioning (NO invoice triggering yet — Session 13; NO email delivery of the PDF yet — Notification session; PDF is generated and stored on S3 only):
    - PDF ENGINE SWAP: spatie/laravel-pdf is Chromium/Browsershot-only (no pure-PHP path) and Chromium is absent locally AND on the VPS → would always fail. Switched to barryvdh/laravel-dompdf (pure PHP, no system binaries). spatie/laravel-pdf left installed but unused. CLAUDE.md tech-stack PDF line updated. Verified dompdf renders the Blade to a valid %PDF- byte stream with no Chromium
    - Migration add_notes_to_agreements_table (nullable text `notes`, plain `php artisan migrate`, never fresh) — the table's only schema change this session; everything else was already on the agreements table from Session 4. Added TYPES / BILLING_CYCLES array constants + `notes` to fillable on the model
    - CreateAgreementDTO (fromRequest + fromAgreement + toAttributes; rate/bond_amount CENTS; carries NO status/version/parent — those are decided by the service). fromAgreement() rebuilds a DTO from an existing row (normalises dates to Y-m-d) as the basis for a new version
    - AgreementService — the ONLY sanctioned lifecycle path, all 3 ops in DB::transaction:
      - create(): status=draft, version=1, parent null; fires AgreementCreated
      - sign(): guards status===draft else throws AgreementNotSignableException; stores signature_data as-is (base64 canvas data URL), signed_at=now(), status=signed (the ONE sanctioned in-place mutation — a status transition, not an edit); fires AgreementSigned; dispatches GenerateAgreementPdfJob AFTER commit (queue worker has no bound tenant, so it must read a persisted row)
      - createNewVersion(): NEW row (never mutates), parent_agreement_id=old id, version=old+1, status=draft (needs re-sign); fires AgreementVersionCreated(old,new)
    - Events AgreementCreated / AgreementSigned / AgreementVersionCreated (no listeners yet — PDF is dispatched DIRECTLY from sign(), not via a listener)
    - AgreementNotSignableException (app/Exceptions) — renders 422 JSON {success:false, message, code:'AGREEMENT_NOT_SIGNABLE'}
    - GenerateAgreementPdfJob (ShouldQueue, onQueue('pdf'), tries=1) — takes agreementId+tenantId (ids only). BINDS current_tenant for the job's duration so Agreement/Customer/Vehicle TenantScope resolves, then forgets it in finally (no leak to the next job). Renders pdf.agreement via dompdf, stores to S3 disk at tenants/{tenant_id}/agreements/{agreement_id}/agreement-v{version}.pdf, sets pdf_path. FAILURE POLICY: logs + returns, NEVER throws — PDF generation must not break the signing flow (signature is already persisted)
    - resources/views/pdf/agreement.blade.php — functional dompdf-friendly template (simple tables/inline styles, no flex/grid), all fields + embedded signature image. Design pass later
    - AgreementPolicy (viewAny/create + view/update/sign/createVersion same-tenant; NO update/delete ability beyond — agreements are immutable). Registered via Gate::policy in AppServiceProvider
    - AgreementController (thin; Gate::forUser(auth('tenant')->user()) on EVERY action — same load-bearing pattern as Fleet/Customer/CRM): index (?status validated vs 5 statuses + ?type vs TYPES + ?search by customer name via whereHas — name is NOT encrypted so LIKE works — + per-status tab counts in one grouped query + paginate 15->withQueryString), create (props: non-archived customers + AVAILABLE vehicles only + types + billingCycles), store, show (eager customer/vehicle + full version lineage tree walked root→descendants, newest-first), sign, createVersion (rebuilds DTO from current via fromAgreement — bypasses StoreAgreementRequest so an original past start_date never blocks re-versioning), downloadPdf (read-only S3 stream; pdf_path null => 404)
    - StoreAgreementRequest — customer_id/vehicle_id exist scoped EXPLICITLY to tenant_id (raw Rule::exists bypasses TenantScope); vehicle_id additionally must be status=available + not soft-deleted; type/billing_cycle in model constants; rate int min 1; bond nullable int min 0; start_date after_or_equal:today (STORE PATH ONLY — versioning is exempt); end_date after start_date; notes nullable. SignAgreementRequest — signature_data required + regex ^data:image/...;base64, (keeps garbage out) + withValidator draft-status guard (Inertia-friendly error; service still re-guards as the authority)
    - routes/tenant.php: Route::resource('agreements')->except(edit,update,destroy) — IMMUTABLE, no edit/delete — + POST {agreement}/sign + POST {agreement}/version + GET {agreement}/pdf, all under existing auth:tenant. {agreement} binds via TenantScope (cross-tenant id => 404). 7 routes
    - resources/js/composables/useCurrency.js — formatAUD(cents)/toCents(dollars)/toDollars(cents), Intl en-AU; reusable across Agreement/Invoice/Customer. Vue Pages/Agreement/{Index,Create,Show}.vue (AppLayout, i18n agreement.* + common.*): Index = status tabs w/ counts + type filter + customer search + status badges; Create = full form, searchable customer dropdown (client filter), available-vehicles-only dropdown, rate/bond entered in AUD → cents on submit via form.transform; Show = full detail + version-history list + inline HTML5 canvas signature pad (mouse/touch draw + Clear, toDataURL→sign POST, draft only) + Create New Version (signed/active) + PDF download link (when pdf_path set). FUNCTIONAL ONLY — design pass later. i18n: agreement.* in en.json + common.agreement.* flash + lang/en/agreement.php validation strings
  - [Verified] Tinker smoke (23 checks, wrapped in a rolled-back txn — DB left clean): create→draft/v1/no-parent + notes + bond cents + AgreementCreated; sign→signed + signature stored + signed_at + AgreementSigned + GenerateAgreementPdfJob asserted pushed ON the 'pdf' queue (Queue::fake); re-sign signed→AgreementNotSignableException renders 422 + code AGREEMENT_NOT_SIGNABLE; new version→new id/v2/parent set/draft/terms copied while OLD row stays signed+v1 + AgreementVersionCreated; cross-tenant Agreement::find returns null (scoped). dompdf renders the Blade to a valid 878KB %PDF- stream with NO Chromium. Full Pest suite 15 green; npm build green; 7 routes registered (no edit/update/destroy)
  - [Done] Invoice triggering on sign — GenerateFirstInvoice listener on AgreementSigned (sync, version-1-only guard, resilient); see Notification System
  - [ ] Email delivery of the PDF as an attachment (Notification session wired a "signed" notice that links to the portal, not the PDF itself), agreement templates (super-admin defaults + tenant customisation), vehicle-change prorating + auto-version + customer re-sign trigger, agreement activation/completion/cancellation transitions (later sessions)
- [In Progress] Invoice & Financial Engine
  - [Done] Ledger foundation (append-only system of record):
    - ledger_entries migration (HasTenant; customer_id restrictOnDelete; created_by → users nullOnDelete; 9 type strings; amount integer cents; reference_type/reference_id loose pointer; indexes on (tenant_id, customer_id) and (reference_type, reference_id))
    - SIGN CONVENTION: amount POSITIVE = debit/charge (owes more), NEGATIVE = credit/payment (owes less); balance = SUM(amount)
    - LedgerEntry model: append-only enforced IN CODE at 3 layers — instance update()/delete() overrides, updating/deleting model-event guards, and a custom LedgerEntryBuilder blocking query-builder mass update()/delete(); UPDATED_AT = null; forCustomer scope
    - LedgerImmutableException (plain RuntimeException — internal invariant, no HTTP render)
    - LedgerService: append() is the ONLY sanctioned write path (validates type); getBalance() sums tenant-scoped entries; created_by left null (system) until auth wired
  - [Done] Invoice structure (foundation only — NO prorating, NO late fees, NO payment gateway):
    - invoices migration (HasTenant; customer_id + agreement_id restrictOnDelete; type recurring/manual/prorated; status draft/sent/paid/overdue/cancelled default draft; subtotal/total/paid_amount cents; indexes on (tenant_id, status) and (tenant_id, due_date))
    - invoice_items migration (invoice_id cascadeOnDelete; HasTenant; vehicle_id restrictOnDelete — reporting link must survive; nullable period_start/period_end)
    - Invoice model (belongsTo Customer/Agreement, hasMany items; isPaid/isOverdue/outstandingAmount)
    - InvoiceItem model (belongsTo Invoice/Vehicle)
    - CreateInvoiceService + CreateInvoiceDTO: creates invoice+items in DB::transaction, total = sum(item amounts), appends rental_charge ledger entry (reference invoice), fires InvoiceGenerated; no notifications yet
    - InvoiceGenerated event (no listeners yet — Notification session)
  - [Verified] Tinker: 2-item invoice → matching ledger entry + getBalance; all 3 immutability layers throw (incl. query-builder bypass); multi-invoice summing; negative payment lowers balance; tenant isolation
  - [Done] Full Invoice Engine — recurring generation, prorated vehicle-change splits, late-fee automation, manual payment recording (NO Stripe/PayPal UI yet — manual cash/bank_transfer only this session):
    - Migration add_next_billing_date_to_agreements_table (date, nullable, plain migrate — never fresh) + Agreement fillable/date-cast. next_billing_date is OPERATIONAL billing metadata, NOT a term — writing it (in sign() and after each generated invoice) does NOT breach agreement immutability (same category as pdf_path)
    - NextBillingDateService (PURE date logic, NO DB, unit-testable): daily → +1d; weekly → next occurrence of billing_cycle_day weekday name (Carbon::next, strictly-after; falls back +1wk when unset); monthly → same day-of-month as billing_cycle_day in next month, CLAMPED to month length (a "31" → Feb 28). Throws on unknown cycle
    - RecurringInvoiceService: generateForAgreement() = the FIRST invoice (period from start_date; does NOT touch next_billing_date — sign() seeds it). generateDue() = scheduled path; LOOPS Tenant::all() binding each (cron has NO bound tenant → bare query would throw TenantNotResolved), finds signed/active agreements with next_billing_date<=today, raises the period invoice + advances next_billing_date. NEVER throws / never aborts the batch (per-tenant + per-agreement try/catch → log + continue). Delegates creation to CreateInvoiceService; queues GenerateInvoicePdfJob. There is no agreement-activation transition yet, so 'signed' counts as billable (D7)
    - AgreementService::sign() now injects NextBillingDateService and seeds next_billing_date inside the existing sanctioned sign transaction (the date the 2nd invoice falls due)
    - GenerateRecurringInvoicesCommand (invoices:generate-recurring) + ApplyLateFeeCommand (invoices:apply-late-fees). Both registered in routes/console.php via Schedule::command(...)->dailyAt('00:01'/'00:05')->timezone('Australia/Sydney')->withoutOverlapping()
    - ProrationService (PURE calc — READS the open invoice, NO writes; safe for preview): splits the ORIGINAL invoice total by integer days — old=intdiv(total*oldDays,totalDays), new=total-old (remainder absorbs the cent) so the two ALWAYS sum EXACTLY to the original total (CLAUDE.md non-negotiable; D2). New vehicle's own rate takes over from the NEXT period via the new version, NOT this in-flight split. changeDate clamped into the period; divide-by-zero guarded. currentOpenInvoice() = latest non-paid/non-cancelled invoice for the agreement
    - VehicleChangeService::execute() — ALL in ONE DB::transaction: prorate → void open invoice (status=cancelled) + post a COMPENSATING ledger credit of −original_total (ledger is append-only, the original +total charge cannot be deleted → must be reversed; D3) → raise 2 prorated invoices via CreateInvoiceService (each AUTO-appends its own charge, so we do NOT double-append) → AgreementService::createNewVersion() with new vehicle_id (immutable v+1 draft, needs re-sign) → old vehicle→available / new→rented via ChangeVehicleStatusAction → fire VehicleChanged. NET ledger effect: +orig −orig +old +new(=orig) = +orig (balance unchanged, correctly re-attributed). Guards: agreement signed/active, new vehicle available + different. Throws VehicleChangeNotAllowedException (422 JSON; controller catches → Inertia back+error)
    - LateFeeService::applyDueLateFees() — LOOPS tenants binding each; grace period per-tenant (settings.late_fee_grace_days, default 3); finds open (draft/sent/overdue) invoices still owing (whereColumn paid_amount<total) past due_date+grace; IDEMPOTENT — skips invoices that already carry a "Late fee" line item (whereDoesntHave), so the daily run never stacks fees (D5). NEVER throws
    - ApplyLateFeeAction::execute() — fee from tenant settings (late_fee_type fixed|percentage; late_fee_amount cents OR late_fee_percentage of total; default fixed 5000c=$50); adds a "Late fee" InvoiceItem (the idempotency marker), bumps invoice subtotal/total, appends positive late_fee ledger entry, status→overdue, fires LateFeeApplied. DB::transaction. Invoices are MUTABLE financial docs (unlike the append-only ledger), so editing subtotal/total/status here is correct
    - RecordPaymentAction::execute(RecordPaymentDTO) — manual payment (cash/bank_transfer); guards not-cancelled (backstop; request validates too); creates Payment (recorded_by=auth('tenant')->id()), appends NEGATIVE payment ledger entry (payment REDUCES balance), bumps paid_amount, status→paid once paid_amount>=total, fires PaymentReceived. DB::transaction
    - payments migration (tenantId() macro; invoice_id/customer_id restrictOnDelete; amount cents; method cash/bank_transfer/stripe/paypal; gateway_payment_id nullable [future gateways]; notes; recorded_by→tenant_users nullOnDelete; paid_at; index (tenant_id, invoice_id)) + Payment model (HasTenant; METHOD_* constants; belongsTo Invoice/Customer/recordedBy). Invoice->payments() hasMany added
    - Events LateFeeApplied (invoice+amount) / PaymentReceived (invoice+payment) — no listeners yet (Notification session). VehicleChangeNotAllowedException (app/Exceptions, 422 JSON, code VEHICLE_CHANGE_NOT_ALLOWED)
    - GenerateInvoicePdfJob (ShouldQueue, onQueue('pdf'), tries=1) — ids only; binds current_tenant for the job then forgets in finally; renders pdf.invoice (dompdf, pure PHP — same approach as agreements) to S3 tenants/{tid}/invoices/{id}/invoice.pdf; sets pdf_path. FAILURE POLICY: logs + returns, never throws (PDF must not break billing). Dispatched after recurring generation + after a payment (regenerate). resources/views/pdf/invoice.blade.php (dompdf-friendly tables, items+totals+payments)
    - InvoiceController (thin; Gate::forUser(auth('tenant')->user()) on EVERY action — same load-bearing pattern as Fleet/Customer/Agreement): index (?status filter vs STATUSES + per-status tab counts in one grouped query + ?search by customer name via whereHas + paginate 15->withQueryString), show (items.vehicle + payments.recordedBy + agreement + customer ledger balance via LedgerService), recordPayment → RecordPaymentAction (+queue PDF), markOverdue (manual override; never overrides paid/cancelled), changeVehicle (TWO-STEP: preview posts confirm=false → ProrationService::calculate ONLY [pure, NO writes] → flashes proration_preview; confirm posts confirm=true → VehicleChangeService::execute → redirect to new version), downloadPdf (read-only S3 stream; null=>404). InvoicePolicy (viewAny/view/recordPayment/markOverdue same-tenant) registered in AppServiceProvider
    - RecordPaymentRequest (amount int min 1, method in Payment::METHODS, notes/paid_at nullable; withValidator guards cancelled invoice). ChangeVehicleRequest (new_vehicle_id exists scoped EXPLICITLY to tenant_id + status available + not soft-deleted; change_date date; confirm bool → isConfirmed())
    - HandleInertiaRequests: added a lazy flash 'proration_preview' prop (the two-step change preview is delivered via session flash, scoped to the agreement in the Vue layer)
    - routes/tenant.php (auth:tenant): Route::resource('invoices')->only(index,show) ({invoice} binds via TenantScope→404) + POST invoices/{invoice}/payment + /overdue + GET invoices/{invoice}/pdf + POST agreements/{agreement}/change-vehicle (on InvoiceController — it is fundamentally a proration op; authorized via AgreementPolicy::createVersion). 6 routes
    - Vue Pages/Invoice/{Index,Show}.vue (AppLayout, i18n invoice.* + common.*): Index = status tabs w/ counts + customer search + total/paid/outstanding columns + badges; Show = summary, line items, totals (subtotal/total/paid/outstanding) + customer ledger balance, record-payment form (AUD→cents via form.transform), payment history, late-fee badge, manual overdue button, PDF link. Agreement Show.vue gained the Change-Vehicle two-step UI (available-vehicle dropdown excl. current + change date → Preview shows prorated split [no writes] → Confirm). useCurrency reused. amounts entered AUD, stored cents. FUNCTIONAL ONLY — design pass later. i18n: invoice.* + agreement.change_*/proration_* in locales/en.json; common.invoice.* flash in lang/en/common.php; lang/en/invoice.php validation strings
  - [Verified] Tinker smoke (29 checks, rolled-back txn — DB left clean): NextBillingDateService daily/weekly(wed)/monthly(15)/monthly(31→Feb clamp); sign sets next_billing_date=2026-08-15 + AgreementSigned; generateForAgreement → recurring invoice, total==rate, correct period, ledger +30000; full payment → paid + paid_amount + Payment row + ledger 0 + PaymentReceived; vehicle change → old invoice cancelled, 2 prorated invoices type=prorated SUMMING EXACTLY to original total, new version v2+new vehicle+draft, old→available/new→rented, ledger NET unchanged + VehicleChanged; late fee → +5000 default, line item present, status overdue, ledger +5000, LateFeeApplied; tenant B cannot see tenant A invoice/payments. Full Pest suite 15 green (53 assertions); npm build green; 6 invoice routes + 2 scheduled commands registered
  - [Done] Expense tracking + expense/GST reports + profit per vehicle — Session 32
  - [ ] Stripe/PayPal payment UI + hosted payment page, payment-link via SMS/email, bond refund calculations, broader financial summaries (later sessions)
- [In Progress] Customer Management
  - [Done] Customer model + migration (HasTenant; licence_number/passport_number encrypted casts; blacklisted/active scopes; date_of_birth/licence_expiry date casts; index on tenant_id+is_blacklisted)
  - [Done] hasOutstandingBalance() wired to the ledger — SELF-CONTAINED, deliberately NOT via LedgerService: it drops TenantScope (withoutGlobalScope) and constrains by the model's OWN tenant_id + customer_id, so the SUM(amount) > 0 check is correct & tenant-safe in ANY context (queue jobs, scheduled commands) where current_tenant is not bound. LedgerService::getBalance relies on the bound tenant and is only used inside web requests (controller show()).
  - [Done] Customer Management UI — controllers/routes/Vue (CRUD + blacklist, no Customer core-logic changes):
    - SoftDeletes: migration add_soft_deletes_to_customers_table + SoftDeletes trait on Customer. NO hard deletes — destroy() soft-deletes only. Soft delete is an UPDATE (sets deleted_at), so it never trips agreements.customer_id / ledger_entries.customer_id restrictOnDelete FKs; a customer with history can still be archived. Trashed rows auto-excluded from route-model binding (→404) and the index list
    - CustomerCreated / CustomerBlacklisted / CustomerUnblacklisted events (no listeners yet)
    - CreateCustomerDTO + UpdateCustomerDTO (fromRequest + toAttributes; NO tenant_id — HasTenant auto-fills; NO blacklist fields — those mutate only via the blacklist actions)
    - CreateCustomerAction — PlanEnforcementService::check('max_customers', Customer::count()) BEFORE insert (hard block; count tenant-scoped & excludes soft-deleted). NOTE: 'max_customers' is an UNKNOWN plan-limit key today → Plan::getLimit() returns -1 (unlimited), so the check is a verified no-op until a plan defines it; verified to hard-block once the limit IS set
    - UpdateCustomerAction — thin $customer->update(); carries no blacklist fields so it can never bypass the blacklist actions
    - BlacklistCustomerAction (sets is_blacklisted + reason, fires CustomerBlacklisted) / UnblacklistCustomerAction (clears both, fires CustomerUnblacklisted) — the ONLY sanctioned blacklist mutation paths
    - StoreCustomerRequest — email unique scoped EXPLICITLY to tenant_id (raw query-builder rule bypasses TenantScope) + whereNull('deleted_at') so a soft-deleted customer's email can be reused. UpdateCustomerRequest extends it with ->ignore($customer->id). BlacklistRequest: reason required|string|max:1000
    - CustomerPolicy (viewAny/create/view/update/delete/blacklist; same-tenant check). Registered via Gate::policy in AppServiceProvider
    - CRITICAL (same as Fleet): controller authorizes via Gate::forUser(auth('tenant')->user())->authorize(...) on EVERY action — NOT $this->authorize() (which resolves the empty web guard). Loud comment block in CustomerController
    - CustomerController (thin): index (?search= over name+phone ONLY — email is encrypted at rest so a SQL LIKE can't match it; ?blacklisted=true|false; paginate(15)->withQueryString; outstanding-balance indicator computed for the page in ONE grouped SUM query, not N per-row lookups), create/store, show (ledger balance via LedgerService::getBalance + empty rentalHistory placeholder), edit/update, destroy (soft), blacklist/unblacklist. Flash via __('common.customer.*')
    - routes/tenant.php: Route::resource('customers') (binds {customer} through TenantScope → cross-tenant id 404) + POST customers/{customer}/blacklist + /unblacklist, all under existing auth:tenant. 9 routes registered
    - Vue Pages/Customer/{Index,Create,Edit,Show}.vue (AppLayout, useForm, all strings via i18n customer.* + common.*). Index: search box + all/active/blacklisted tabs, blacklist badge, balance indicator, RED TINT on blacklisted rows. Show: full profile, balance formatted cents→AUD (Intl en-AU), blacklist form / unblacklist button (own endpoints), rental-history placeholder. Edit slices ISO date casts → YYYY-MM-DD for <input type=date>. FUNCTIONAL ONLY — design pass later
  - [Verified] Tinker smoke (10 checks): CustomerCreated fires; max_customers=1 hard-blocks 2nd create; licence_number encrypted in DB but decrypts via model; blacklist sets flag+reason & fires event; unblacklist clears & fires event; name search filters; soft delete hides row but keeps deleted_at; ledger charge → hasOutstandingBalance() true; tenant B cannot see tenant A's customer (even withTrashed); hasOutstandingBalance() works with NO current_tenant bound (no TenantNotResolved). Full Pest suite 15 green; npm build green
  - [Done] Identity documents (licence front/back, proof of address) — private disk, signed-URL download, plan file-size hard block. See Session 28
  - [ ] Bond/security-deposit logic, CRM lead-conversion auto-create (later sessions)
- [Done] Customer Portal (the 5th, fully isolated guard — end customers log in to view invoices/agreements, pay, track rentals)
  - [Done] Data layer (2 migrations, plain `php artisan migrate` — never fresh):
    - customer_users (HasTenant via tenantId() macro; customer_id → customers restrictOnDelete [the AUTH account, distinct from the Customer profile/ledger record]; email + password; rememberToken; softDeletes; UNIQUE(tenant_id, email) — per-tenant, NOT global)
    - customer_portal_invitations (HasTenant; customer_id → customers restrictOnDelete; email; token UNIQUE + indexed [Str::uuid]; accepted_at nullable; expires_at [7-day window set by the action])
  - [Done] Models: CustomerUser (extends Authenticatable; HasTenant + Notifiable; guard_name='customer'; password hashed cast; belongsTo Customer — NO Spatie roles, single role) · CustomerPortalInvitation (HasTenant; belongsTo Customer; isExpired()/isAccepted())
  - [Done] config/auth.php: 'customers' provider repointed App\Models\User → CustomerUser. customer is the FIFTH dedicated guard model (after tenant, superadmin, mechanic) — fully isolated, never mixed
  - [Done] InviteCustomerToPortalAction — the ONE invite path; idempotent on two fronts (existing CustomerUser → return latest invite untouched; outstanding unaccepted+unexpired invite → reuse rather than mint a duplicate token); else creates a 7-day invite + fires CustomerPortalInvitationSent (tenant_id explicit)
  - [Done] CustomerPortalInvitationSent event (holds Customer + invitation; no listener yet — invite-link delivery is a later Notification session, matching every other portal event)
  - [Done] Auth: CustomerAuthController (showLogin/login [tenant-scoped attempt — explicit tenant_id ON TOP of TenantScope, so one tenant's customer creds can't authenticate against another; 5/email+ip+slug rate limit]/logout) + CustomerLoginRequest (slug included in throttle key so a lockout on one tenant's portal never blocks a different customer sharing the email)
  - [Done] Public invitation acceptance: CustomerPortalController::showAcceptInvitation/acceptInvitation (web.php, NO customer.tenant middleware → tenant NOT bound; resolves+binds it from {tenant_slug}). CRITICAL (per requirement): the invitation lookup is withoutGlobalScope + WHERE token AND explicit tenant_id — a token from tenant A can NEVER be redeemed on tenant B's portal URL. Validates not accepted (410) / not expired (410); accept creates the CustomerUser + marks accepted_at in a DB::transaction, logs in via the customer guard, redirects to dashboard
  - [Done] CustomerPortalController (authenticated; Gate::forUser(auth('customer')->user()) on every record action): dashboard (current rental = newest signed/active latest-version agreement + outstanding balance via LedgerService + last 3 invoices), invoices (own customer_id, paginate 15), showInvoice (items.vehicle + payments + agreement), downloadInvoicePdf, agreements, showAgreement, downloadAgreementPdf, makePayment (reuses RecordPaymentAction — recorded_by is null for a self-service payment). EVERY action scoped to the authed customer's OWN customer_id — never another customer's data
  - [Done] PDF downloads via SHORT-LIVED SIGNED S3 URLs ONLY (Storage::disk('s3')->temporaryUrl, 15-min expiry, redirect away) — never a direct S3 path; null pdf_path → 404
  - [Done] CustomerPortalPolicy (viewInvoice/viewAgreement own-customer_id + tenant_id; makePayment = own invoice AND not cancelled). Registered as Gate::define delegations (viewPortalInvoice/viewPortalAgreement/makePortalPayment) — NOT Gate::policy, because Invoice/Agreement already map to their own policies (a model has one policy); same model-less delegation pattern as SuperAdminPolicy
  - [Done] MakePaymentRequest (amount int min 1; method in cash|bank_transfer — manual only, no Stripe/PayPal yet; notes nullable) · AcceptInvitationRequest (password required|min 8|confirmed)
  - [Done] Middleware ResolveTenantForCustomer (sibling of ResolveTenantForMechanic — binds current_tenant + shares ONLY the customer-guard auth payload + slim tenant; never mixes the tenant guard). CRITICAL FIX: it forgets the {tenant_slug} route param after binding. The leading prefix param is not declared by authed controller methods; left in place, Laravel's positional dependency resolution shifts it into the next argument (a route-model Invoice/Agreement param receives the slug string → TypeError). Controllers read the slug from current_tenant, never the route param
  - [Done] bootstrap/app.php: alias 'customer.tenant' → ResolveTenantForCustomer; customer route group middleware now ['web','customer.tenant']; ResolveTenantForCustomer pinned into the priority list before AuthenticatesRequests (same fix as TenantMiddleware/ResolveTenantForMechanic) so it binds before auth:customer loads the tenant-scoped CustomerUser. redirectGuestsTo now has ALL FOUR branches: /superadmin/* → superadmin.login (handled first, before the current_tenant check — platform-wide, no slug); then by bound current_tenant: /portal/* → customer.login, /mechanic/* → mechanic.login, else → tenant.login
  - [Done] makePayment captures the slug BEFORE running RecordPaymentAction: PaymentReceived fans out to a notification listener that binds+forgets the tenant in a finally block; under a sync queue that runs inline and would otherwise leave current_tenant unbound for the redirect
  - [Done] Routes: web.php public GET/POST portal/{slug}/invite/{token} (customer.portal.invite[.accept]). customer.php (['web','customer.tenant']): login GET/POST, logout + behind auth:customer → dashboard, invoices index/show/pdf/pay, agreements index/show/pdf ({invoice}/{agreement} bind via TenantScope → cross-tenant 404). tenant.php: POST customers/{customer}/invite-portal → CustomerController::invitePortal (idempotent; surfaces "already has access"); Customer/Show now passes hasPortalAccess. 14 customer routes + 1 tenant invite route
  - [Done] Vue (FUNCTIONAL ONLY — design pass later; all strings i18n customer.portal.*): CustomerLayout REBUILT (own header + dashboard/invoices/agreements nav + logout + mobile nav + flash; never shares AppLayout/SuperAdmin/Mechanic). Pages/Customer/Portal/{Login (PublicLayout), AcceptInvitation (PublicLayout — set-password form, slug/token/email props), Dashboard (current-rental + outstanding-balance + last-3-invoices cards), Invoices/{Index (status badges + pay/view), Show (totals + line items + payment history + pay-now form, AUD→cents via useCurrency)}, Agreements/{Index, Show (PDF link)}}. Customer/Show.vue gained the portal-access section (Invite button → POST invite-portal, or "has access" badge). i18n: customer.portal.* + customer.invite_portal/has_portal_access/portal_access(_hint) in en.json; common.customer.portal_* flash strings in lang/en/common.php
  - [Verified] tests/Feature/CustomerPortalTest.php (DatabaseTransactions — never fresh; real HTTP stack): invite creates invitation + fires CustomerPortalInvitationSent; accept creates CustomerUser + marks accepted + auto-login on customer guard; tenant-A token REJECTED (404) on tenant B's portal URL (the token+tenant_id requirement); expired invite → 410; customer login; creds isolated per tenant (A's password rejected at B); customer sees own invoice (200) but NOT another customer's same-tenant invoice (403 policy); guest → customer.login (redirectGuestsTo portal branch + priority pin); customer guard CANNOT reach tenant admin routes (→ tenant login, wrong guard); make payment → invoice paid + Payment row + ledger. 10 tests / full Pest suite 42 green (141 assertions); npm build green; 14 customer + 1 invite route registered
  - [Done] Customer password reset (Session 21 — tenant-scoped broker; reset link path embeds {tenant_slug}) — see Session 21 below
  - [ ] Invite-link delivery via email/SMS (CustomerPortalInvitationSent listener — Notification session); Stripe/PayPal online payments + hosted payment page; rental-status detail once the Rental module lands
- [In Progress] Workshop & Mechanic Portal
  - [Done] Data layer (3 migrations, plain `php artisan migrate` — never fresh):
    - mechanics (HasTenant via tenantId() macro; name/email/phone(nullable); pin + password BOTH nullable+hashed [login needs at least one]; is_active default true; last_login_at; rememberToken; softDeletes; UNIQUE(tenant_id, email) — per-tenant, NOT global)
    - service_logs (HasTenant; vehicle_id + mechanic_id restrictOnDelete; 5-status string; title/description; odometer_reading nullable; labour_cost + total_cost cents; started_at/completed_at; softDeletes; indexes (tenant_id,vehicle_id) + (tenant_id,status))
    - parts_used (HasTenant; service_log_id cascadeOnDelete; quantity/unit_cost/total_cost cents; index (tenant_id,service_log_id))
  - [Done] Models: Mechanic (extends Authenticatable; HasTenant + Spatie HasRoles guard_name='mechanic' + Notifiable + SoftDeletes; pin/password hashed casts; ROLE_MECHANIC/ROLE_SENIOR; scopeActive; serviceLogs hasMany) · ServiceLog (HasTenant; 5 STATUS_* consts + STATUSES; belongsTo Vehicle/Mechanic, hasMany parts; isComplete(); totalPartsCost()) · PartUsed (HasTenant; $table='parts_used'; saving hook auto-sets total_cost = quantity × unit_cost)
  - [Done] config/auth.php: 'mechanics' provider repointed App\Models\User → Mechanic (was scaffold). Mechanic guard is the FOURTH guard, fully separate from tenant/superadmin/customer — never mixed
  - [Done] Vehicle: serviceLogs() hasMany + lastServiceLog() hasOne latestOfMany() added
  - [Done] Actions:
    - GenerateVehicleQrAction — DETERMINISTIC token hash_hmac('sha256', vehicle->id, app.key) (regenerate is idempotent → printed sticker never invalidated); renders **SVG** (dependency-free — PNG needs imagick, absent on VPS) encoding route('mechanic.scan', slug+token); stores S3 tenants/{tid}/vehicles/{id}/qr.svg; static qrPath() shared with the stream endpoint. Sets qr_code_token (fills the pre-existing nullable+unique vehicles column)
    - CreateServiceLogAction — DB::transaction; resolves vehicle scoped; creates log (pending, started_at, mechanic_id from the AUTHED guard not input); vehicle → maintenance via ChangeVehicleStatusAction (the ONLY sanctioned status path); fires MaintenanceStarted
    - ChangeServiceLogStatusAction — the ONE status path; validates vs STATUSES; refreshes total_cost = labour + sum(parts) on every transition; on transition INTO completed (guarded against re-complete) stamps completed_at, returns vehicle → available via ChangeVehicleStatusAction, fires MaintenanceCompleted
    - AddPartAction — DB::transaction; creates PartUsed (total via model hook) then rolls log total_cost = labour + all parts
  - [Done] Events MaintenanceStarted / MaintenanceCompleted (hold ServiceLog; no listeners yet — notifications/analytics later)
  - [Done] Auth: MechanicAuthController (showLogin/login/logout). Password path → Auth::guard('mechanic')->attempt(email+password+tenant_id+is_active=true). PIN path → EXPLICIT Mechanic::withoutGlobalScope(TenantScope)->where(tenant_id)->where(email)->where(is_active)->first() + Hash::check(pin) + manual guard login (NEVER Auth::attempt for PIN — it only checks password). Stamps last_login_at. Honours a QR-scan token stashed in the session → returns the mechanic to that vehicle after login. MechanicLoginRequest (email required; password/pin each required_without the other; 5/email+ip rate limit, mirrors tenant LoginRequest)
  - [Done] Public QR scan: QrScanController@scan (web.php, NO tenant/auth middleware → tenant NOT bound). Resolves tenant from slug + BINDS it (mandatory before both the scope-free vehicle lookup AND the mechanic-guard check — the Mechanic model is tenant-scoped). Vehicle found via withoutGlobalScope(TenantScope)+explicit tenant_id by qr_code_token (404 if none). Authed mechanic → redirect mechanic.vehicle; guest → stash token + redirect mechanic.login. Exposes no data
  - [Done] MechanicPortalController (mechanic guard; Gate::forUser(auth('mechanic')->user()) on EVERY mutating action): dashboard (own active + recent-completed jobs, vehicle eager-loaded WITH qr_code_token for deep-links), scanResult=mechanic.vehicle (vehicle by token + history + new-log form), createLog (resolves vehicle by the page's token, CreateServiceLogAction), updateStatus (ChangeServiceLogStatusAction), addPart (AddPartAction)
  - [Done] WorkshopController (tenant admin, READ-ONLY; Gate::forUser(auth('tenant')->user())): index (status + vehicle filters, per-status counts, vehicle dropdown), show, vehicleHistory (Index preset to one vehicle). Service logs are created/mutated ONLY from the mechanic portal
  - [Done] MechanicPolicy (registered ServiceLog → MechanicPolicy in AppServiceProvider). READ abilities (viewAny/view) typed Authenticatable so BOTH guards work (Mechanic for the portal, TenantUser for the admin view — both carry tenant_id); MUTATING abilities (create/update/addPart) typed Mechanic, with ownership: own log only unless ROLE_SENIOR
  - [Done] Middleware/wiring (bootstrap/app.php): NEW ResolveTenantForMechanic (binds current_tenant for the mechanic group + shares ONLY the mechanic-guard auth payload — deliberately NOT TenantMiddleware, which shares the tenant-guard user and would mix guards). Mechanic group middleware now ['web','mechanic.tenant']. ResolveTenantForMechanic ALSO pinned into the priority list before AuthenticatesRequests (same fix as TenantMiddleware) so it binds before auth:mechanic loads the tenant-scoped Mechanic. redirectGuestsTo now BRANCHES: mechanic/* → mechanic.login, else tenant.login
  - [Done] Routes: web.php public GET mechanic/{slug}/scan/{token} (mechanic.scan — the QR target). mechanic.php (['web','mechanic.tenant']): login GET/POST, logout, dashboard, vehicle/{token} (mechanic.vehicle — DISTINCT path from the public scan to avoid collision), logs POST + logs/{log}/status PUT + logs/{log}/parts POST behind auth:mechanic ({log} binds via TenantScope → 404). tenant.php: workshop index/show/vehicle history + POST/GET fleet/{vehicle}/qr (generate + inline SVG stream, admin-only)
  - [Done] MechanicRolesSeeder (mechanic/senior_mechanic under guard 'mechanic'; idempotent; NOT wired into DatabaseSeeder — run on demand). i18n: workshop.* + auth.pin/use_pin/use_password in locales/en.json; common.workshop.* + common.fleet.qr_generated in lang/en/common.php
  - [Done] Vue (FUNCTIONAL ONLY — design pass later): MechanicLayout enhanced (header + logout, mechanic-guard auth). Pages/Workshop/Mechanic/{Login (PIN/password toggle, PublicLayout), Dashboard (active + completed jobs), ScanResult (vehicle + history + new-log form + per-log status buttons + add-part forms, AUD→cents via useCurrency)}. Pages/Workshop/{Index (status tabs + counts + vehicle filter + table, AppLayout), Show (summary + parts table + cost breakdown)}. Fleet/Show gained the QR section (inline SVG via stream endpoint + generate/regenerate) + a service-history list
  - [Verified] Bootstrapped action/model smoke (24 checks, rolled-back txn — DB clean): PIN+password hashed & verify; scopeActive; QR token == deterministic HMAC + SVG stored on (faked) S3 + regenerate keeps token; create log → pending + started_at + mechanic_id from authed guard + vehicle→maintenance + MaintenanceStarted + total==labour; add part → 2×5000=10000 + log total 13000; complete → completed_at + vehicle→available + total stable + MaintenanceCompleted; invalid status throws; tenant B cannot see tenant A's mechanic/log/vehicle
  - [Verified] tests/Feature/WorkshopTest.php (DatabaseTransactions — never fresh; real HTTP stack): password login; PIN login; inactive mechanic rejected; credentials isolated per tenant; guest mechanic → mechanic.login (validates redirectGuestsTo branch + priority pin); public scan guest → login + token stashed; unknown token 404; authed scan → vehicle; createLog via HTTP → vehicle maintenance + 1 log; tenant-A mechanic session NOT authenticated at tenant B. 10 tests / full Pest suite 25 green (81 assertions); npm build green; 13 workshop/mechanic/QR routes registered (no collision)
  - [Done] QR-scan signing (Session 21 — public scan route is now a permanent Laravel signed URL; tamper → 403, legacy unsigned sticker → friendly 410 page; `vehicles:regenerate-qr-codes` command) — see Session 21 below
  - [Done] Mechanic CRUD UI (Session 21 — tenant-admin MechanicController + ManageMechanicPolicy; no more tinker/seeder) — see Session 21 below
  - [ ] Workshop analytics dashboard, service scheduling, photo/document upload, maintenance notifications (MaintenanceStarted/Completed listeners)
- [Done] AI Assistant (tenant-restricted; two modes; Groq/Qwen/DeepSeek + Log fallback; AI calls SYNCHRONOUS this session per the build spec — not queued)
  - [Done] Data layer (2 migrations, plain `php artisan migrate` — never fresh):
    - ai_conversations (HasTenant via tenantId() macro; user_id → tenant_users nullOnDelete [keep history if staff removed]; mode help|intelligence; title nullable [auto from first message]; timestamps + softDeletes; index (tenant_id, user_id))
    - ai_messages (HasTenant; conversation_id → ai_conversations cascadeOnDelete; role user|assistant; content text; tokens_used nullable int [usage tracking]; provider nullable [which provider answered]; index (tenant_id, conversation_id))
  - [Done] Models: AiConversation (HasTenant + SoftDeletes; MODE_HELP/MODE_INTELLIGENCE consts + MODES; belongsTo TenantUser(user); hasMany messages; latestMessage() hasOne latestOfMany — serialised as `latest_message` for the Index preview) · AiMessage (HasTenant; ROLE_USER/ROLE_ASSISTANT consts; belongsTo conversation; tokens_used int cast)
  - [Done] Provider contract REWRITTEN: AiProviderInterface now query(array $messages, string $systemPrompt, int $tenantId): string + maxTokens(): int. CONTRACT: query() NEVER throws — catches all transport/API/parse errors → logs + returns a graceful message string (the user's message is already persisted, so a provider outage can't break the flow). $messages is OpenAI chat format (oldest→newest). Safe breaking change — nothing implemented the old one-arg stub yet
  - [Done] Providers (app/Modules/AI/Providers): AbstractAiProvider (implements interface; holds name() [stable key for attribution] + lastTokensUsed() — kept OFF the public interface per spec, used by AiService to record the provider that ACTUALLY answered incl. 'log' fallback) → AbstractHttpAiProvider (shared OpenAI-compatible Http-client call: system prompt prepended as role:'system'; temperature 0.4; parses choices.0.message.content + usage.total_tokens; empty-key guard; logs+graceful-string on failure/empty) → GroqAiProvider (api.groq.com/openai/v1, llama-3.3-70b-versatile, maxTokens 8192) · QwenAiProvider (DashScope compatible-mode, qwen-turbo, 6144) · DeepSeekAiProvider (api.deepseek.com, deepseek-chat, 4096). LogAiProvider (extends AbstractAiProvider; null-object — returns the fixed "not configured" notice, 0 tokens, never calls out)
  - [Done] AiProviderFactory::make(Tenant): AiProviderInterface — driven by tenant->settings['ai_provider']; CRITICAL credential-gated Log fallback (mirrors NotificationProviderFactory): a real provider is returned only when its key is present in config/services (groq/qwen/deepseek.key), else LogAiProvider — "Log by default; real provider activates the moment its key lands in .env" with zero code change, and never calls a paid endpoint with an empty bearer. config/services.php gained groq/qwen/deepseek key entries (GROQ_API_KEY/QWEN_API_KEY/DEEPSEEK_API_KEY)
  - [Done] Context builders (app/Modules/AI/Context): HelpModeContext::systemPrompt() — STATIC DVARO feature/usage prompt (fleet, customers, agreements, invoices, CRM, workshop, notifications, portal, billing), NO tenant data at all. IntelligenceModeContext::systemPrompt(Tenant) — builds a FRESH (never cached) tenant-scoped snapshot every call: vehicle total + per-status breakdown, customers + blacklisted, active/signed agreements, outstanding invoice balance, overdue count, revenue this month (= payments since the 1st), top-3 vehicles by revenue (paid invoices), vehicles due for service (next 30 days), last-5 payments. All money formatted AUD ($ + number_format), never raw cents. Prompt hard-instructs: answer using ONLY this data; access to "[tenant name]" ONLY; refuse cross-company questions
  - [Done] TENANT-ISOLATION REQUIREMENT (explicit this session) on the top-3-vehicles query: runs inside an authed request with current_tenant bound, so TenantScope auto-constrains invoice_items — withoutGlobalScope is NOT used anywhere here (unlike the public controllers). TenantScope qualifies its column (invoice_items.tenant_id) so there is no ambiguity in the join; an EXPLICIT where('invoice_items.tenant_id', $tenant->id) is added too as cross-table defence. PAID filter via whereHas('invoice') (Invoice carries TenantScope → stays scoped). The joined `vehicles` table is NOT covered by any global scope, so tenant_id is filtered EXPLICITLY inside the join clause (->where('vehicles.tenant_id', $tenant->id)) — mandatory; + whereNull(vehicles.deleted_at) since a raw join doesn't honour SoftDeletes
  - [Done] AiService::chat(AiConversationDTO): AiMessage — scoped to current tenant AND current tenant user. Resolves/creates the conversation, fills title from the first message (covers both creation paths), persists the USER message FIRST (must survive a provider error), builds a bounded history window (last 10 messages oldest→newest), picks the system prompt by mode (intelligence → IntelligenceModeContext fresh snapshot), calls provider->query(), persists the assistant reply recording the provider that ACTUALLY answered + tokens_used, fires AiQueryProcessed. Provider call is deliberately NOT wrapped in a DB transaction (sync outbound HTTP that never throws — nothing to roll back, don't hold a txn across the round-trip). startConversation(mode,userId) is the explicit "new thread" path (title filled on first turn)
  - [Done] AiConversationDTO (conversation_id nullable=new / mode / message; fromRequest) · AiQueryProcessed event (holds AiMessage; NO listeners yet — future usage/cost tracking; not registered in EventServiceProvider, matching the listener-less-event convention)
  - [Done] AiPolicy (Gate::policy(AiConversation)) — TWO layers: same-tenant (defence-in-depth on top of TenantScope binding) AND same-user (own conversations only — staff A can never read staff B's chats, which may contain BI data). viewAny/create true; view/chat/delete = ownsWithinTenant. Evaluated against the TENANT guard via Gate::forUser(auth('tenant')->user()) on EVERY action
  - [Done] AiChatRequest (conversation_id nullable + Rule::exists scoped EXPLICITLY to tenant_id [raw query bypasses TenantScope] + whereNull(deleted_at); mode in MODES; message required max 2000) · ai-chat named limiter in AppServiceProvider = 20/min keyed by tenant_id:user_id (AI costs money — throttled hard; IP fallback if unauthenticated)
  - [Done] EnsureTenantHasModule middleware (alias 'tenant.module') — reusable HARD BLOCK: abort 403 unless current_tenant->hasModule($module). Applied PER ROUTE as 'tenant.module:ai' (see routing note). TenantOnboardingService seeds ai_provider='log' into the tenant settings defaults (inside the existing onboarding transaction)
  - [Done] AiController (thin; Gate::forUser(auth('tenant')->user()) on every action; scoped to tenant AND current user): index (own conversations + latestMessage → Inertia AI/Index), show ({conversation} binds via TenantScope → cross-tenant 404 → AI/Show with messages), chat (AiChatRequest → AiService → JSON response, NOT Inertia — async client), newConversation (mode-validated → startConversation → redirect to show), destroy (soft delete). routes/tenant.php: GET ai, POST ai/new, POST ai/chat (throttle:ai-chat, returns JSON), GET ai/{conversation}, DELETE ai/{conversation} — all behind auth:tenant + 'tenant.module:ai' applied per-route
  - [Done] CRITICAL BUG FIX (shared) — TenantMiddleware now calls $request->route()?->forgetParameter('tenant_slug') after binding (the SAME fix ResolveTenantForCustomer/Mechanic already had; TenantMiddleware was missing it). Without it, the leading {tenant_slug} prefix param shifts into the controller's route-model argument under positional dependency resolution, so GET ai/{conversation} (and every other tenant {model} show route) received the slug STRING → TypeError. The tenant-app {model} show routes had simply never been HTTP-tested before, so the latent bug went unnoticed. Controllers read the slug from current_tenant, never the route param, so forgetting it is safe — full suite confirms no regression
  - [Done] Vue (FUNCTIONAL ONLY — design pass later; AppLayout; i18n ai.*): Pages/AI/Index.vue (conversation list w/ mode badges + latest-message preview; "New Help chat" / "New Intelligence chat" buttons → POST ai/new; delete) · Pages/AI/Show.vue (chat-bubble thread user-right/assistant-left + mode badge; async window.axios POST to /ai/chat — appends reply with NO page reload; optimistic user bubble rolled back on error; loading "thinking" state; 429 → rate-limit message; auto-scroll). i18n: ai.* in locales/en.json + common.ai.conversation_deleted in lang/en/common.php + lang/en/ai.php (provider not_configured/error/unavailable + module_not_in_plan messages)
  - [Verified] tests/Feature/AiAssistantTest.php (DatabaseTransactions — never fresh; real HTTP stack): module gate 403s when plan excludes 'ai' + 200 when included; chat persists conversation + user/assistant messages, provider='log' by default, reply is the "not configured" notice, title from first message; user A CANNOT view user B's conversation in the SAME tenant (403 policy); cross-tenant conversation id 404s (TenantScope binding); IntelligenceModeContext includes tenant A's own data (name, Total vehicles: 1, Available: 1, the service-due reg) and NEVER tenant B's (reg + name absent) — the explicit cross-table isolation requirement; rate limit blocks the 21st request/min (429). 7 tests / full Pest suite 49 green (179 assertions); npm build green; 5 AI routes registered. Tinker smoke: AiProviderFactory returns Log when groq selected w/o key, Groq when key set, Log on default; LogAiProvider returns the fixed notice + 0 tokens
  - [ ] Async/streaming responses + queued AI calls (kept synchronous this session by design); per-tenant token/cost accounting via an AiQueryProcessed listener; tenant AI-provider selection UI (settings.ai_provider editable via a screen — set via tinker/onboarding default today); conversation rename; Qwen/DeepSeek live-key smoke against the real APIs
- [In Progress] Notification System
  - [Done] Data layer: notification_logs migration (HasTenant; loose notifiable_type/_id — NOT a morphTo, so a log outlives the record it references and can span guards; channel/event_type/recipient/subject/body/status/provider/provider_message_id/error_message/sent_at; indexes (tenant_id,status) + (notifiable_type,notifiable_id)) + NotificationLog model (STATUS_*/CHANNEL_*/TYPE_* consts incl. TYPE_VEHICLE/TYPE_AGREEMENT; failed/sent/forNotifiable scopes). Follow-up migration made notifiable_* NULLABLE so future tenant-level events (plan-limit, trial-expiring) log without a fake notifiable
  - [Done] Provider interfaces (SmsProviderInterface/EmailProviderInterface/WhatsAppProviderInterface) — CONTRACT: send() NEVER throws, returns bool. Implementations (9): ClickSend/Cellcast/Log (SMS), Mailgun/Resend/SMTP/Log (email), ClickSend/Log (WhatsApp); real HTTP via Http facade, all errors→log+false
  - [Done] NotificationProviderFactory — tenant->settings driven (sms_provider/email_provider); CRITICAL credential-gated Log fallback (a real provider is only returned when its creds are actually present, else Log) → "Log by default, real providers activate when .env creds land" with zero code change. WhatsApp platform-wide ClickSend (not tenant-selectable), same Log fallback
  - [Done] NotificationService — single dispatch entry point (sendSms/sendEmail/sendWhatsApp); writes EXACTLY ONE NotificationLog row per attempt (success or failure); NEVER throws (provider throw → failed row + false); tenant_id set explicitly so writes are correct with no bound tenant
  - [Done] Templates: FormatsNotifications trait (money cents→$, d/m/Y AU date, email_html wrapper — inline styles, email-client-safe) + NotificationContent value object (subject + emailBody HTML + smsBody + whatsapp fallback; immutable, pure, unit-testable) + 6 templates: AgreementSigned, InvoiceGenerated, PaymentReceived, LateFeeApplied, LeadSubmitted, ExpiryReminder (generic over the 4 expiry kinds). English-only message copy (notification BODY i18n deferred per CLAUDE.md)
  - [Done] Listeners: QueuedNotificationListener abstract base (ShouldQueue, queue 'notifications'; injects NotificationService; bindTenant/forgetTenant — queue worker has no bound tenant, models restore via newQueryWithoutScopes but RELATIONS need the bind; channel rule = email always-on when enabled+address, SMS/WhatsApp only if tenant-enabled AND phone present; notifyCustomer / notifyAdmin [admin = tenant_admin email-only, no phone field]). 5 queued listeners: SendAgreementSignedNotification (→customer), SendInvoiceGeneratedNotification (→customer), SendPaymentReceivedNotification (→customer), SendLateFeeNotification (→customer), SendLeadSubmittedNotification (→admin)
  - [Done] GenerateFirstInvoice (Invoice module listener on AgreementSigned) — fulfils CLAUDE.md "invoice creation on agreement sign". SYNCHRONOUS (not queued — invoice must exist by redirect; local write), ordered FIRST on AgreementSigned. GUARD: only version 1 / no parent bills (a re-signed vehicle-change version v2+ must NOT re-bill — VehicleChangeService already raised its prorated invoices). RESILIENT: failure logged, never rethrown, so it can't roll back the signature. Its InvoiceGenerated then drives the customer invoice notice
  - [Done] EventServiceProvider (shouldDiscoverEvents=false; explicit $listen map) registered in bootstrap/providers.php. AgreementSigned→[GenerateFirstInvoice(sync), SendAgreementSignedNotification(queued)]; InvoiceGenerated/PaymentReceived/LateFeeApplied/LeadSubmitted→their queued listeners
  - [Done] TenantOnboardingService seeds notification settings on tenant create: email_provider=log, sms_provider=log, notify_email_enabled=true, notify_sms_enabled=false, notify_whatsapp_enabled=false (inside the existing onboarding transaction)
  - [Done] NotificationSettingsController (tenant_admin-ONLY via direct auth('tenant') role check — settings are tenant-wide config, no per-record policy target) + UpdateNotificationSettingsRequest (EMAIL_PROVIDERS/SMS_PROVIDERS const lists) + routes GET/PUT app/{slug}/notifications/settings (tenant.notifications.settings[.update]) + Pages/Notification/Settings.vue (AppLayout, useForm, i18n notifications.*; provider dropdowns + 3 channel toggles). Merges into tenant.settings preserving unrelated keys. i18n: notifications.* in en.json + common.notifications.saved
  - [Done] ExpiryReminderService (→admin email) + SendExpiryRemindersCommand (notifications:send-expiry-reminders, scheduled dailyAt 00:10 Australia/Sydney withoutOverlapping). SINGLE 14-day window (exact date match, one reminder per entity — no 30/14/7 ladder). Covers vehicle registration_expiry/insurance_expiry/next_service_due + agreement end_date (signed/active). Tenant-loop binding each (cron has no bound tenant); NEVER throws (per-tenant try/catch); IDEMPOTENT via notification_logs dedupe (same notifiable+event_type+today → skip), gated by notify_email_enabled
  - [Verified] Tinker smoke (16 checks, rolled-back txn — DB left clean): all 6 templates render subject+email+sms with correct AUD/labels; factory returns Log providers by default + falls back to Log when clicksend selected w/o creds; SendPaymentReceivedNotification → 1 email log (provider=log, status=sent) + 0 sms when sms disabled, sms log appears once toggle flipped; SendLeadSubmittedNotification → admin email; ExpiryReminderService sends on the 14-day insurance window + dedupes on a 2nd same-day run. Full Pest suite 15 green (53 assertions); npm build green; 2 notification routes + event map + scheduled command registered
  - [ ] Email delivery of the agreement/invoice PDF as an attachment (signed notice currently links to the portal, does not attach the PDF); deferred triggers — plan-limit-reached, trial-expiring, subscription-renewed, bond-refunded, registration-expiry vs the broader CLAUDE.md trigger list; notification log UI; per-trigger channel matrix
- [Done] Reporting & Analytics (revenue/fleet/overdue/workshop/customers/maintenance reports; Redis cache-aside; queued PDF + Excel exports; Australian FY)
  - [Done] composer require phpoffice/phpspreadsheet (Excel export engine; dompdf already present for PDF)
  - [Done] ReportingService (app/Modules/Reporting/Services) — every analytics query, ALL tenant-scoped (relies on bound current_tenant + HasTenant; NO withoutGlobalScope anywhere — isolation must hold), ALL money returned as integer CENTS, ALL ranges accept Carbon. Aggregation pushed into SQL (GROUP BY, to_char for monthly buckets) for VPS memory frugality:
    - revenueByPeriod — revenue per month by PAYMENT RECEIPT date (sums Payment.amount grouped by month of paid_at; per the approved change #1, revenue belongs to the period cash was received, NOT invoice issue date)
    - revenueByVehicle — SUM(invoice_items.amount) for items on PAID invoices issued in range, grouped by vehicle (withTrashed for archived vehicles' history) + de-duplicated days_rented from agreements
    - fleetUtilisation — per live vehicle: total_days in window, days_rented (merged signed/active agreement overlaps), utilisation %
    - overduePayments — invoices status=overdue OR (status != paid/cancelled AND due_date < today); customer, total, outstanding, days_overdue; filters outstanding>0
    - workshopPerformance — completed logs in range: total jobs, labour/parts cents, avg duration (hours), per-mechanic breakdown
    - customerGrowth — new customers per month (withTrashed — a signup still happened)
    - maintenanceCosts — labour+parts per vehicle for completed logs in range
    - dashboardStats — revenue this month, active rentals, overdue count+total, fleet utilisation %, vehicles due service (next 30d)
    - tenantDashboardSummary — outstanding balance (SUM(total-paid_amount) on non-paid/cancelled invoices), active rentals, vehicles available (for the Session-8 dashboard; NOT cached — cheap counts, read fresh)
    - australianFY — current AU FY (1 Jul–30 Jun) in Australia/Sydney: month>=7 → starts this year, else last year
    - rentedDaysForVehicle helper MERGES overlapping agreement-version intervals so a rental day is never double-counted; open-ended agreements clamp to window end
  - [Done] ReportCacheService — cache-aside wrapper; controller reads through THIS, never ReportingService directly (CLAUDE.md "never hit DB on every page load"). Key report:{tenant_id}:{type}:{md5(params)}; TTL 30min (5min for overdue). Cache TAGS deliberately AVOIDED (default store is `database`, tags unsupported; tests use `array`) — instead a per-(tenant,type) KEY INDEX is tracked so invalidate() can forget every variant; store-agnostic. invalidate(type, ?tenantId) clears a type for a tenant. remember() logs a report.cache hit/miss+ms line (the verify-step timing log)
  - [Done] ExportService — exportPdf (renders reports.{type} Blade via dompdf, pure PHP) + exportExcel (PhpSpreadsheet → .xlsx); both store to S3 tenants/{id}/reports/{type}-{Y-m-d-His}.{ext} and return the path; ONLY called from the queued job. TYPES=[revenue,fleet,overdue,workshop]. tabulate() flattens each report to columns+rows for Excel (money cents→plain "1234.56"); PDF blades consume the raw report arrays
  - [Done] GenerateReportExportJob (app/Jobs; ShouldQueue, onQueue('exports'), tries=1) — ids only (reportExportId+tenantId); binds current_tenant for the job then forgets in finally (mirrors GenerateInvoicePdfJob). Assembles report data by type via ReportingService, calls ExportService, updates the ReportExport to ready+file_path, fires ReportExportReady. On throw → status=failed (never rethrows). failed() hook also marks failed so the UI never waits forever
  - [Done] report_exports migration (tenantId() macro; requested_by → tenant_users nullOnDelete; report_type/format/parameters(json)/status(default pending)/file_path nullable/expires_at; index (tenant_id,status)) + ReportExport model (HasTenant; STATUS_* PENDING/READY/FAILED + FORMAT_* PDF/EXCEL; parameters array + expires_at datetime casts; isReady/isExpired; requestedBy belongsTo)
  - [Done] ReportExportReady event (holds ReportExport; NO listener yet — requester notification is a later session; listener-less, not in EventServiceProvider)
  - [Done] ReportCacheInvalidationListener (per approved change #2) — SYNC listener on PaymentReceived; busts revenue + overdue caches for the EXACT tenant (by $event->payment->tenant_id, no reliance on bound tenant); registered in EventServiceProvider alongside the existing SendPaymentReceivedNotification (kept simple — one listener, two invalidate calls)
  - [Done] ReportingController (thin; Gate::forUser(auth('tenant')->user()) on EVERY action — same load-bearing pattern as Fleet/Invoice/AI; reads via ReportCacheService): dashboard, revenue, fleet, overdue, workshop, customers, maintenance, export (pre-creates pending ReportExport with expires_at=+24h so the UI has an id immediately, dispatches the job, flashes queued message), downloadExport ({export} binds via TenantScope→404; own-tenant + ready + not-expired else 404/410 → 15-min signed S3 temporaryUrl redirect). recentExports() feeds each page a download list
  - [Done] ReportingPolicy (viewAny/export role-free + download own-tenant) registered via Gate::policy in AppServiceProvider · ReportRequest (from/to nullable date; from<to + ≤2yr span in withValidator; range() defaults to AU FY) · ExportRequest extends it (report_type in ExportService::TYPES, format in pdf/excel)
  - [Done] report-export named limiter (5/hour PER TENANT, keyed by bound tenant id; IP fallback) registered in AppServiceProvider; applied as throttle:report-export on POST reports/export (per approved change #3 — route throttle, not in the FormRequest)
  - [Done] reports Blade views (resources/views/reports/{revenue,fleet,overdue,workshop}.blade.php + shared partials/head+foot) — dompdf-friendly tables, tenant name + generated timestamp, money cents→$. Functional, not styled
  - [Done] routes/tenant.php (auth:tenant): GET reports + reports/{revenue,fleet,overdue,workshop,customers,maintenance}; POST reports/export (throttle:report-export); GET reports/exports/{export} (declared AFTER the literals to avoid shadowing). 9 routes
  - [Done] Vue (FUNCTIONAL ONLY — design pass later; AppLayout; i18n reporting.*): Pages/Reporting/{Dashboard (5 KPI cards), Revenue (pure CSS/HTML bar chart — no chart lib per VPS rule — + by-vehicle table), Fleet (colour-coded utilisation green>70/yellow40-70/red<40), Overdue (no date range), Workshop (totals + by-mechanic), Customers, Maintenance}. Shared Components/Reporting/{ReportNav (sub-nav), ReportToolbar (date range + PDF/Excel export buttons + recent-exports download list)}. Amounts via useCurrency. i18n: reporting.* + dashboard.outstanding_balance/active_rentals/vehicles_available/view_reports in en.json; common.reporting.export_queued in lang/en/common.php; lang/en/reporting.php (range validation)
  - [Done] Tenant main Dashboard (Session 8) extended: TenantDashboardController injects ReportingService → adds 'summary' (outstanding balance, active rentals, vehicles available); Tenant/Dashboard.vue shows the 3 cards + a "View reports" link (existing planName/subscriptionStatus/trialDaysRemaining props untouched)
  - [Verified] tests/Feature/ReportingTest.php (DatabaseTransactions — never fresh): revenue report is tenant-isolated (A sees only its $500, B's $999.99 never leaks, and vice-versa); australianFY resolves correctly either side of 1 July (2025-06-20 → FY24/25, 2025-07-10 → FY25/26); export dispatches GenerateReportExportJob ON the 'exports' queue + pre-creates a pending ReportExport (Queue::fake). 3 tests / full Pest suite 52 green (189 assertions); npm build green; 9 reporting routes registered. Tinker smoke (Storage::fake s3, rolled-back txn, 10 checks): revenue total, cache hit serves identical data on 2nd call, FY both halves, invalidate(revenue) clears the index, PDF + Excel export jobs → status ready with non-empty S3 files, .xlsx extension
  - [ ] Auto-invalidate revenue_by_vehicle/dashboard caches on payment (only revenue+overdue busted per spec — others age out on TTL); export-ready notification (ReportExportReady listener); scheduled cleanup of expired export files from S3; most-profitable-vehicle / customer-default deeper analytics; design pass
- [Done] Landing Website + CMS (public marketing site + super-admin content control; PLATFORM-WIDE, no tenant_id anywhere; pricing always live plan data)
  - [Done] Data layer (2 migrations, plain `php artisan migrate` — never fresh):
    - cms_content_blocks (NO tenant_id — platform-wide; key UNIQUE; type text|richtext|image; content text nullable [text/richtext]; image_path nullable [S3 path for image type]; section string + INDEX; sort_order int default 0; timestamps)
    - demo_requests (NO tenant_id — pre-tenant lead capture; company_name/contact_name/email; phone/message nullable; status new|contacted|converted default new; timestamps)
  - [Done] Models: CmsContentBlock (app/Modules/CMS/Models; NO HasTenant — never TenantScope; TYPE_TEXT/RICHTEXT/IMAGE consts; isImage(); imageUrl() = Storage::disk('s3')->url(image_path) for image blocks else null; value() = image URL for image blocks else raw content — the single accessor the service/pages read) · DemoRequest (STATUS_* consts; plain fillable)
  - [Done] CmsContentService (app/Modules/CMS/Services) — CACHE-FIRST, mirrors PlatformSettingsService: get(key,default) (Redis Cache::remember, key 'cms:'+key, 1h TTL, "__missing__" sentinel so a genuine miss isn't re-queried) returns the block's value() (image URL or text); getSection(section) (cached collection of {key,type,value}, key 'cms:section:'+section); update(key,value) (writes content, busts BOTH the per-key AND the section cache); updateImage(key, UploadedFile) (stores S3 cms/images/{key}.{ext} — deterministic path, re-upload replaces — sets image_path, busts both caches). Cache clears IMMEDIATELY on every write (CLAUDE.md non-negotiable)
  - [Done] CmsContentSeeder (database/seeders; IDEMPOTENT firstOrCreate by key; NOT wired into DatabaseSeeder — run on demand). Seeds 25 blocks across hero (heading/subheading/cta_text/image), features (6 blocks × title+description = 12), about (heading + richtext body), contact (email/phone/address), testimonials (2 × quote+author). English marketing copy; pricing is NEVER seeded (always live Plan data)
  - [Done] Super admin CMS editor: CmsContentController (app/Modules/SuperAdmin/Http/Controllers; thin; Gate::forUser(auth('superadmin')->user())->authorize('contentAccess') on EVERY action — the load-bearing forUser pattern, NEVER $this->authorize()): index (all blocks grouped by section → SuperAdmin/Cms/Index), update (text/richtext), updateImage (S3 upload). contentAccess gate = platform_owner OR content_manager (existing SuperAdminPolicy, Session 16). UpdateCmsContentRequest (merges route {key} into data, exists:cms_content_blocks, content required max 5000) + UpdateCmsImageRequest (key exists AND type=image, image required mimes jpg/jpeg/png/webp max 5MB)
  - [Done] Public site: PublicLandingController (app/Modules/CMS/Http/Controllers; PUBLIC — no auth, no tenant middleware): index (hero/features/about/testimonials/contact section maps + activePlans → Public/Landing), pricing (activePlans → Public/Pricing), about, contact, submitContact (ContactFormRequest → persisted AS a DemoRequest [name→company_name+contact_name, enquiry→message] + DemoRequestSubmitted, per the approved "reuse demo_requests" decision). activePlans() = Plan::where('is_active',true)->orderBy('sort_order') mapped to a public-safe array (prices stay CENTS; modules/limits passed through) — inactive plans NEVER shown, pricing NEVER hardcoded
  - [Done] DemoRequestController (CMS, public) — store(DemoRequestFormRequest): creates DemoRequest (status new) + fires DemoRequestSubmitted. DemoRequestFormRequest (company_name/contact_name/email required; phone/message nullable). DemoRequestSubmitted event (app/Modules/CMS/Events; holds DemoRequest; NO listener yet — future super-admin alert; listener-less, NOT in EventServiceProvider per convention)
  - [Done] DemoRequestController (SuperAdmin) — index (?status validated vs STATUSES + per-status counts in one grouped query + paginate 20 → SuperAdmin/DemoRequests/Index), markContacted ({demoRequest} binds implicitly — platform-wide, no TenantScope). contentAccess-gated
  - [Done] Rate limiting: 'public-forms' named limiter (3/hour PER IP — unauthenticated pre-tenant forms, nothing to key on but IP) in AppServiceProvider; applied as throttle:public-forms on POST /contact + /demo-request
  - [Done] Routes: web.php PUBLIC (no auth, no tenant) — GET / (landing, replaced the welcome closure), /pricing, /about, /contact + POST /contact + POST /demo-request (both throttle:public-forms). superadmin.php (behind superadmin.auth) — GET cms, PUT cms/{key}, POST cms/{key}/image, GET demo-requests, POST demo-requests/{demoRequest}/contacted
  - [Done] SEO: resources/js/composables/useSeo.js — builds document title + a deduped meta set (description + og:title/og:description/og:type) keyed by head-key so navigating swaps tags rather than stacking. Each Public page renders distinct title+meta via <Head>
  - [Done] PublicLayout REBUILT (was a bare slot scaffold): sticky top nav (logo + Pricing/About/Contact + Log in/Get started), responsive mobile hamburger menu, footer (product/company link columns + copyright). Dark+light, i18n public.*. Used by landing/pricing/about/contact + tenant login/register. NOTE: tenant login is path-based (/app/{slug}/login) with no global entry yet, so both "Log in" and "Get started" route to /register — a global login/workspace-resolver page is a follow-up
  - [Done] Vue (FUNCTIONAL ONLY — design pass later; i18n public.* for chrome, CMS content English per the approved decision): Pages/Public/{Landing (hero + 6 feature cards from CMS + 3-plan pricing preview + testimonials-if-present + demo CTA), Pricing (monthly/annual toggle, every active plan, Get-Started→/register), About (richtext body via v-html — trusted super-admin source), Contact (CMS details + contact form)} + Components/Public/{DemoRequestForm (posts /demo-request), PlanCard (cents→AUD via useCurrency, module list via superadmin.plans.module_keys i18n, live data)}. SuperAdminLayout nav gained cms + demo-requests. Pages/SuperAdmin/{Cms/Index (section tabs; text/richtext inline textarea + per-block Save [PUT]; image preview + upload [POST]), DemoRequests/Index (status tabs + counts + table + mark-contacted)}. i18n: public.* + superadmin.cms.*/demo_requests.* + nav keys in en.json; common.cms.* flash in lang/en/common.php
  - [Verified] tests/Feature/CmsLandingTest.php (DatabaseTransactions — never fresh; real HTTP stack): landing renders Public/Landing with seeded hero content; pricing shows ONLY active plans (inactive hidden — pre-existing dev plans neutralised in-txn for determinism); demo request captured + DemoRequestSubmitted fired; demo request rate-limited (4th/IP/hour → 429); contact form persisted as a DemoRequest (name→company+contact, enquiry→message); super admin CMS text update busts cache (next read returns new value); CMS image upload stores to (faked) S3 + sets image_path; CMS editor content-gated (billing_manager 403, content_manager 200). 8 tests / 42 assertions. Full Pest suite was 52 green before; +8 CMS = 60 green; npm build green (Landing/Pricing/About/Contact compiled); 6 public (/, pricing, about, GET+POST contact, demo-request) + 5 super-admin CMS/demo routes registered; CmsContentSeeder seeds 25 blocks across 5 sections
  - [Done] Global tenant login / workspace-slug resolver page (Session 21 — /find-workspace; "Log in" nav now points here) — see Session 21 below
  - [Done] CMS image public-read ACL on upload (Session 21 — code change; bucket policy is a Session 22 infra task) — see Session 21 below
  - [ ] Stripe/PayPal checkout from the pricing CTA (Get-Started currently → /register only); DemoRequestSubmitted listener (super-admin email/notification); rich-text WYSIWYG editor (textarea today); CMS public-bucket policy (infra) + old-image cleanup; demo→tenant one-click conversion; design pass

## Session 21 — Security & correctness backlog (bug-fix/gap-closing; no new features, no design)
- [Done] forgetParameter audit (CRITICAL): ALL tenant-resolving middleware now call `$request->route()?->forgetParameter('tenant_slug')` after binding. **Found + fixed: ResolveTenantForMechanic was MISSING it** (TenantMiddleware + ResolveTenantForCustomer already had it). Superadmin has NO {tenant_slug} (platform-wide) → no equivalent needed. tests/Feature/ForgetParameterTest.php: one test PER GUARD hitting a {model} route (NOT an index) — tenant GET fleet/{vehicle}, mechanic PUT logs/{log}/status (would TypeError 500 without the mechanic fix), customer GET invoices/{invoice}, superadmin GET tenants/{tenant} (no-slug regression guard). 4 tests green
- [Done] Signed QR URLs (CLAUDE.md "QR codes signed + expirable"): GenerateVehicleQrAction now encodes URL::signedRoute('mechanic.scan', …) (PERMANENT signature — printed stickers must not rot; HMAC qr_code_token kept as the DB lookup key). QrScanController validates hasValidSignature() FIRST: valid → proceed; signature present but invalid → TAMPERED → abort 403; NO signature param at all → LEGACY pre-signing sticker → friendly Inertia Workshop/QrExpired page (410, "contact your rental company"), never a raw error. Console command `vehicles:regenerate-qr-codes` (loops Tenant::all, binds each, regenerates — idempotent token, refreshes the stored signed-URL SVG; per-vehicle + per-tenant try/catch, never aborts the batch). WorkshopTest updated to sign scan URLs + 2 new tests (tampered → 403, legacy → 410)
- [Done] Mechanic CRUD UI (tenant-admin; replaces tinker/seeder-only creation): MechanicController (index/create/store/edit/update/destroy[soft]) + ManageMechanicPolicy (tenant_admin ONLY — DISTINCT from MechanicPolicy which governs ServiceLogs; registered Gate::policy(Mechanic::class)) + Create/UpdateMechanicAction + DTOs (null pin/password omitted so the 'hashed' cast never sees null; blank-on-edit keeps the stored hash) + Store/UpdateMechanicRequest (email unique per-tenant + whereNull(deleted_at); store enforces ≥1 credential via required_without pair; edit makes both optional). Route::resource('mechanics')->except('show') under auth:tenant. Vue Mechanic/{Index,Create,Edit}.vue (AppLayout, i18n mechanic.*). "Manage mechanics" link added to the tenant Dashboard hub (AppLayout has no global nav). No plan-limit metering on mechanics this session (out of scope). tests/Feature/MechanicManagementTest.php: admin creates → mechanic can log in (hashing verified); non-admin staff → 403; blank-credential edit keeps password; soft delete. 4 tests green
- [Done] Password reset — 3 tenant-scoped guards (tenant, customer, mechanic). **Super admin reset is manual/support-only by design (NOT built)**, confirmed with the client. Reusable abstract App\Http\Controllers\Auth\PortalPasswordResetController (showRequestForm/sendResetLink/showResetForm/reset) + 3 thin guard subclasses (Tenant/Customer/Mechanic PasswordResetController) binding broker/guard/login-route/views. config/auth.php: 3 new brokers (tenant/customer/mechanic) over one shared `portal_password_reset_tokens` table (migration 2026_06_30_000004). TENANT-SCOPING (the load-bearing security property): App\Traits\ResetsPasswordWithinTenant makes each model's getEmailForPasswordReset() return a COMPOSITE "{guard}:{tenant_id}:{email}" — so a token is stored per-tenant-per-guard and a token issued for tenant A's address can NEVER be redeemed under tenant B (even when both share the literal email); the user is still resolved by the bound tenant (TenantScope on the provider lookup). The reset LINK embeds {tenant_slug} in its PATH (/app|portal|mechanic/{slug}/reset-password/{token}?email=…) so the bound tenant is correct when clicked cold from an inbox. Email sent via the EXISTING NotificationService (PortalPasswordResetNotifier → sendEmail; Log provider in dev). Rate limit `password-reset` = 3/email/hour (keyed scope:tenant:email) on each sendResetLink. Vue ForgotPassword.vue + ResetPassword.vue per guard + "Forgot password?" link on all 3 login pages. lang/en/passwords.php (incl. neutral sent_generic — no user enumeration). tests/Feature/PasswordResetTest.php: forgot sends email + stores composite token; reset → login with new password; **cross-tenant token rejected (same email, tenant B)**; customer + mechanic reset; 3/hour rate limit. 6 tests green
- [Done] Global workspace lookup: WorkspaceLookupController (show → Public/FindWorkspace; find → scope-free cross-tenant TenantUser lookup by email — 0 = notFound + register prompt, 1 = redirect to that tenant's login, 2+ = workspace picker; suspended/cancelled tenants excluded). GET/POST /find-workspace (web.php, public; POST throttled `workspace-lookup` 10/hour/IP — curbs enumeration). PublicLayout "Log in" now → /find-workspace (was → /register). Vue Public/FindWorkspace.vue + public.find_workspace.* i18n. tests/Feature/WorkspaceLookupTest.php: single→redirect, multiple→picker, none→notFound, rate limit. 4 tests green
- [Done] CMS image public ACL: CmsContentService::updateImage() now `Storage::disk('s3')->put($path, $contents, 'public')` (public-read ACL — landing assets are served from a plain public S3 URL, unlike tenant docs which use signed URLs). **Requires the S3 bucket policy to allow public reads on the cms/images/* prefix — a deployment task (see below).**
- [Verified] php artisan migrate (plain — never fresh): 1 new table. npm run build green. **Full Pest suite 80 green (318 assertions)** — was ~60 before; +20 this session (ForgetParameter 4, Workshop +2, MechanicManagement 4, PasswordReset 6, WorkspaceLookup 4). No regressions

## Session 22 — Design System Foundation (system/shell only; module page CONTENT deliberately NOT restyled — that's the next session)
- [Decision] Pure MONOCHROME accent (no colour) + Inter font, both client-confirmed. Primary interactive = inverted ink (black button/white text in light, white/black in dark); links via weight/underline; focus = ink ring + offset. Muted status colours (success/warning/danger/info) exist for BADGES ONLY.
- [Done] tailwind.config.js — design tokens, ADDITIVE so un-restyled pages stay pixel-stable: `ink` monochrome scale (50→950, named to not shadow Tailwind `neutral`); `accent` aliased to ink (one-object swap point for a future hue); muted `success/warning/danger/info` palettes (light+dark stops); new type tokens (`2xs`, `display`) + named radii (`control`/`card`) + subtle layered shadows (`subtle`/`card`/`pop`) + `w-18` collapsed-sidebar width + `ease-smooth` + fade/scale/slide keyframes — all added WITHOUT overriding Tailwind defaults. `brand` indigo KEPT this session (some un-restyled pages still reference it; retires next session). darkMode:'class' unchanged
- [Done] Font: Inter loaded via preconnected Google Fonts in app.blade.php (system fallback chain already in fontFamily.sans). Inertia progress bar recoloured to ink-500 (#71717a — visible on both themes)
- [Done] ANTI-FOUC: synchronous inline `<script>` FIRST in app.blade.php `<head>` (no async/defer), before @vite + the render-blocking stylesheet. Reads localStorage('color-mode') first, falls back to prefers-color-scheme, sets `dark` on `<html>` before first paint → no theme flash. body recoloured to ink tokens
- [Done] Per-user dark/light persistence (CLAUDE.md "per-user preference"):
  - Migration 2026_06_30_000005 adds `color_mode` string default 'system' to ALL FOUR guard tables (tenant_users, customer_users, mechanics, super_admins) in one migration; `color_mode` added to each model's $fillable
  - UserPreferenceController@updateColorMode (App\Http\Controllers) — validates in:light,dark,system; DETECTS the active guard (tenant→customer→mechanic→superadmin) and persists on that user; returns 204 (XHR-only). Route PUT preferences/color-mode registered in ALL FOUR guard groups (tenant/customer/mechanic under /…/{tenant_slug}/…, superadmin under /superadmin/…), named {guard}.preferences.color-mode
  - All 4 share middlewares (TenantMiddleware, ResolveTenantForCustomer/Mechanic, SuperAdminMiddleware) now share a uniform `colorMode` Inertia prop {value, url} so the composable reads value + endpoint regardless of guard (null on guest pages)
  - composables/useColorMode.js — module-level singleton; localStorage is the immediate cache (drives the inline script); server `colorMode.value` adopted ONLY on a fresh browser (hadLocalAtInit guard) so a just-made local toggle is never clobbered; every change writes localStorage + silent window.axios.put; tracks OS theme changes while in 'system'; syncFromServer() called onMounted in each authed layout
- [Done] Icons: @heroicons/vue@2.2.0 installed (outline set used). No Icon wrapper — direct tree-shakeable imports (the idiom)
- [Done] Shared UI library (resources/js/Components/UI/): Button (primary/secondary/ghost/danger × sm/md/lg + loading spinner), Input/Select/Textarea (label/error/help slots, v-model, useForm-error ready, useId a11y), StatusBadge (GENERIC variant-driven — success/warning/danger/info/neutral; the legacy fleet Components/StatusBadge.vue is LEFT IN PLACE this session, pages migrate next session), StatCard (KPI + optional trend/icon), PageHeader (title/description/actions slot), EmptyState (icon/title/message/action), Skeleton (pulse), Modal (Teleport + backdrop + Esc + scroll-lock + scale-in), DataTable (head/row slots, built-in loading skeleton + empty state + Inertia pagination footer), Toast (auto-ingests Inertia flash.success/error). composables: useToast.js (singleton store, 4s auto-dismiss), useSidebar.js (collapse persisted to localStorage, collapsed-by-default; mobile-drawer open state)
- [Done] Sidebar.vue — collapsible icon rail; COLLAPSED BY DEFAULT, explicit toggle (in TopBar) NOT hover; collapsed = icons + hover tooltip, expanded = icons + labels; active-route highlight; smooth width transition; mobile = slide-over drawer (Teleport + backdrop). `variant` prop: 'default' (theme-aware) | 'dark' (forced-dark monochrome rail). TopBar.vue — sidebar toggles (collapse on desktop / open drawer on mobile), context title, color-mode sun/moon toggle, notification bell placeholder, user-menu dropdown (initials avatar → profile[if href]/logout, click-outside backdrop)
- [Done] Layout rebuilds (SHELLS ONLY — page bodies untouched): AppLayout (tenant — Sidebar w/ Dashboard/Fleet/Customers/Agreements/Invoices/Leads/Workshop/Reports/AI/Mechanics/Settings nav built from tenant.slug paths + TopBar + Toast + amber→warning impersonation banner preserved [reads shared 'impersonating' prop]); SuperAdminLayout (variant="dark" rail + "Platform" context label = distinct-context cue so admins never confuse portals; Dashboard/Tenants/Plans/Subscriptions/CMS/Demo Requests/Settings); CustomerLayout (TOP NAV, no sidebar — Dashboard/Invoices/Agreements + color toggle + logout; replaced inline flash banners with Toast); MechanicLayout (mobile-first, large 44px touch targets, prominent "Scan" CTA → workshop home [real scan = physical QR sticker via phone camera; in-app scanner is a later session], color toggle, logout); PublicLayout refined to ink tokens + colour toggle in nav/mobile menu (was slate/indigo)
- [Done] i18n: added common.{menu,toggle_sidebar,toggle_theme,notifications,profile,logout} + a `nav.*` namespace (tenant sidebar labels) + workshop.scan to locales/en.json. SuperAdmin reuses superadmin.nav.*, customer reuses customer.portal.*, mechanic reuses workshop.* — no new strings hardcoded
- [Verified] php artisan migrate (plain — never fresh): 1 new column-add migration across 4 tables. npm run build green (AppLayout/SuperAdminLayout/TopBar/PublicLayout chunks compiled). **Full Pest suite 84 green (328 assertions)** — was 80; +4 new UserPreferenceTest (tenant persists color_mode + 204; invalid value 422; super admin persists on no-slug route [guard detection]; guest redirected by auth:tenant). No regressions. 4 preferences.color-mode routes registered (one per guard)
- [ ] NEXT SESSION: restyle module PAGE CONTENT (Fleet/Customer/Agreement/Invoice/CRM/Workshop/Reporting/AI/dashboards) onto the new components; migrate module pages off the legacy fleet StatusBadge to UI/StatusBadge; retire the `brand` indigo palette once no page references it; wire a real user Profile page (TopBar profileHref currently optional/omitted) — **DONE Session 27**; optional in-app QR camera scanner for the mechanic portal
- [ ] MANUAL BROWSER VERIFY (left to the user — needs a running app + seeded login): sidebar collapse/expand persists across reload; dark/light toggle has no FOUC, persists across page loads AND is saved to the profile (new device adopts it); all sidebar nav links resolve; all 4 layouts; mobile drawer + responsive breakpoints

## Session 23 — Design System Page Restyle (frontend-only; NO new functionality, routes, migrations, or backend changes)
- [Decision] Every existing module PAGE migrated onto the Session-22 shared UI library (Components/UI/): Button, DataTable, StatusBadge, PageHeader, EmptyState, StatCard, Input, Select, Textarea, Modal, Skeleton, Toast. Only template markup + tokens changed — every page's useForm field set, submit URL, HTTP method, and handler function was preserved VERBATIM from the functional version. All legacy `slate/indigo/emerald/amber/violet` utility classes replaced with `ink` + muted status tokens; grep of resources/js/Pages + Components confirms ZERO legacy palette classes remain
- [Done] Cross-cutting conventions applied to every page: PageHeader (title/description + #actions slot) replaces raw `<h1>` + `py-10` wrappers (layout owns spacing); lists → DataTable (`:columns` for skeleton/empty colspan, `:pagination` = Laravel paginator → built-in footer, `#empty` → EmptyState); forms → Input/Select/Textarea (label/error/help) + Button `:loading="form.processing"`; amount columns right-aligned + tabular-nums; **hand-rolled flash banners REMOVED on authed pages — the layout Toast auto-ingests flash.success/error** (banners KEPT only on PublicLayout pages, which mount no Toast: Contact, DemoRequestForm, the forgot-password status notices)
- [Done] StatusBadge migration COMPLETE: every module maps its own status enum → a generic variant via a small inline map (fleet available→success/rented→info/maintenance→warning/suspended+reserved→neutral/accident→danger; agreement+invoice draft→neutral/signed+sent→info/active+paid→success/cancelled→neutral/overdue→danger; workshop pending→neutral/in_progress→info/completed→success/waiting_for_parts→warning/re_inspection_required→danger; lead/demo/subscription/tenant maps analogous). **Legacy Components/StatusBadge.vue DELETED** (was fleet-specific, `:status` prop); UI/StatusBadge.vue header comment updated
- [Done] Shared UI enhancements (non-breaking, additive props so wrapper-level `class` fallthrough on the root div still works): Input.vue gained `step/min/max` (numeric fields — rate/bond/pricing/limits), `readonly` (lead intake-link + generated-link displays), `autocomplete` (auth + mechanic pin/password), `inputmode` (mechanic numeric PIN → mobile numeric keypad). No API removed
- [Done] Pages restyled (14 groups): Tenant Dashboard (StatCards); Fleet Index/Show/Create/Edit; Customer Index/Show/Create/Edit; Agreement Index/Show[canvas signature pad + version history + 2-step vehicle-change preview preserved exactly]/Create; Invoice Index/Show[record-payment form]; CRM Lead Index/Show/Create + IntakeForm/IntakeSuccess (English-only per spec); Workshop Index/Show + Mechanic Dashboard/ScanResult (mobile-first); Reporting Dashboard + Revenue/Fleet/Overdue/Workshop/Customers/Maintenance + shared ReportNav/ReportToolbar; AI Index/Show[async axios chat preserved]; SuperAdmin Dashboard/Tenants/Plans(+PlanForm)/Subscriptions/Settings/Cms/DemoRequests/Login; Customer Portal Dashboard/Invoices/Agreements/Login/Forgot/Reset/AcceptInvitation; Public Landing/Pricing/About/Contact; Notification Settings; Mechanic Index/Create/Edit; all tenant/customer/mechanic auth pages + FindWorkspace + QrExpired
- [Deviation from plan] PlanCard: the plan (Q&A) said create Components/UI/PlanCard.vue, but a wired PlanCard already existed at Components/Public/PlanCard.vue (imported by Pricing + Landing). Restyled it IN PLACE with ink tokens rather than duplicate-and-rewire (DRY; avoids churn). No new PlanCard created
- [Verified] `npm run build` green (final + 6 incremental checkpoint builds across the session, all green). Grep confirms no legacy palette classes remain in Pages or Components. **Full Pest suite 84 passed (328 assertions)** — unchanged from Session 22, confirming the frontend-only restyle introduced NO backend regression (the suite exercises the controllers/actions behind all 8 target flows: blacklist, agreement sign, payment record, lead convert, tenant suspend, mechanic login/log, etc.)
- [ ] MANUAL BROWSER VERIFY of the 8 functional paths (Fleet create · Customer blacklist · Agreement sign · Invoice record-payment · Lead convert · AI chat · Portal payment · SuperAdmin suspend) is OUTSTANDING — the dev DB currently holds only leftover Pest test tenants (0 customers/agreements/invoices/leads/mechanics, unknown passwords) so a live click-through needs DemoSeeder + known credentials first; AI chat additionally needs a live provider key. Frontend logic was preserved verbatim and build+Pest are green, but the click-through gate itself has NOT yet been exercised

## Session 24 — Storage: local-filesystem default, S3 optional (deployment blocker; NO new features, NO migrations)
- [Decision] The whole app defaulted to hardcoded `Storage::disk('s3')` and silently failed everywhere without AWS creds. Fixed by making the LOCAL filesystem the working default; S3 is now an optional upgrade via `FILESYSTEM_DISK=s3`. Three client-confirmed decisions: (1) reuse the stock `public` disk — no `dvaro` alias; (2) use the framework's built-in signed `temporaryUrl` on the local disk (`serve => true`) via one `FileUrlService` — NO custom download controller; (3) QR codes + CMS images stay on the local `public` disk PERMANENTLY, even once S3 backs sensitive documents
- [Done] Two disk ROLES, never hardcoded: **sensitive** files (agreement/invoice PDFs, report exports) → `config('filesystems.default')` = `local` (root `storage/app/private`, NOT web-accessible) now / `s3` once configured; **public** files (QR, CMS images) → the `public` disk (`storage/app/public`, served from `/storage/`). `config/filesystems.php` default was ALREADY `env('FILESYSTEM_DISK','local')`; `.env`/`.env.example` ALREADY `FILESYSTEM_DISK=local` — added the `# Change to 's3' when AWS credentials are configured` comment. Added `backup` (Contabo s3-compatible) + `backup-local` disks
- [Done] `App\Services\FileUrlService::temporaryUrl(path, minutes=15)` — the ONE place the driver is branched: wraps `Storage::disk(config('filesystems.default'))->temporaryUrl()`, which works for BOTH s3 (native pre-signed) and local (Laravel 11 `serve => true` → framework-signed route `storage/{path}` named `storage.local`, same short-lived-signed-URL model as S3). Sensitive local files live in `storage/app/private` — NOT under the `public/storage` symlink — so the signed route serves them while the symlink only ever serves genuinely-public assets (no collision)
- [Done] Swapped ALL 12 hardcoded `Storage::disk('s3')` call sites:
  - Sensitive → `config('filesystems.default')`: GenerateAgreementPdfJob, GenerateInvoicePdfJob, ExportService (pdf+xlsx `put`), Agreement/Invoice `downloadPdf` (auth-checked stream — `download()` behaves the same on both drivers), Reporting `downloadExport` + Customer portal `signedPdfRedirect` (both now via FileUrlService). Removed the now-unused `Storage` import from ReportingController + CustomerPortalController
  - Public → `public` disk: GenerateVehicleQrAction (`put`), FleetController `qr` (auth-checked stream), CmsContentService::updateImage (dropped the S3 public-read ACL arg — local `public` visibility handles it), CmsContentBlock::imageUrl (`Storage::disk('public')->url`)
- [Done] `php artisan storage:link` run (public/storage → storage/app/public). Added `storage:link` + `storage:check` to the deploy checklist. SUPERSEDED the old Session-22 S3 public-bucket-policy deploy task (public assets are local now)
- [Done] `config/backup.php` published (spatie/laravel-backup ^9.3); destination + monitor disk conditional on env — `backup` (Contabo) when `CONTABO_BACKUP_KEY` is set, else `backup-local` (storage/backups). App `s3` bucket is never a backup target
- [Done] `App\Console\Commands\StorageCheckCommand` (`php artisan storage:check`) — reports active default + public driver, round-trips a write/read/delete probe on both disks, and (for local) asserts the public/storage symlink exists; non-zero exit on failure for CI
- [Done] CLAUDE.md tech-stack storage line → "Local filesystem (default) or AWS S3-compatible (optional, via FILESYSTEM_DISK env)"
- [Verified] `grep disk('s3')` over app/ = 0 real call sites (only a doc comment in FileUrlService). tests/Feature/CmsLandingTest now fakes the `public` disk. `php artisan storage:check` passes. Bootstrapped smoke: default=local; sensitive file lands under app/private; `FileUrlService::temporaryUrl` returns a signed+expiring `/storage/...?signature=` URL on local; public file lands under app/public with a `/storage/` url; dompdf still emits `%PDF-`. **Full Pest suite 84 passed (328 assertions)** — no regressions. No migrations, no frontend changes (npm build not required)

## Session 25 — Billing & Subscription UI (tenant billing portal + super-admin manual plan assignment / offline payments; NO Stripe — that is Session C)
- [Decision] Tenants can VIEW their plan + usage and REQUEST an upgrade ("contact us" flow — no self-service assignment); super admins manually ASSIGN any plan and RECORD offline payments. Three client-confirmed calls: (1) storage shown as an informational FILE COUNT only — no percentage meter, no fake MB (no byte totals are tracked); progress bars for vehicles/staff/customers only; (2) assignPlan sets tenant status=active + clears trial_ends_at in the same txn (leaving "trial" against a paid sub is a data-integrity bug); (3) customers usage bar included (max_customers is a real enforced limit). SubscriptionUpgraded event did NOT exist (brief assumed it did) — CREATED this session
- [Done] Migrations (plain `php artisan migrate` — never fresh): create_upgrade_requests_table (tenantId; requested_plan_id + current_plan_id[nullable] → plans restrictOnDelete; status default 'pending'; notes; index(tenant_id,status)) · create_subscription_payments_table (tenantId; subscription_id → subscriptions restrictOnDelete; amount CENTS; currency default AUD; method; reference nullable; notes; paid_at; recorded_by → super_admins nullOnDelete; index(tenant_id,subscription_id))
- [Done] Models: UpgradeRequest (HasTenant; STATUS_* pending/contacted/completed/cancelled; requestedPlan/currentPlan belongsTo) · SubscriptionPayment (HasTenant; METHOD_* bank_transfer/cash/stripe/paypal/other; subscription/recordedBy belongsTo). Subscription gained payments() hasMany. Events UpgradeRequested / SubscriptionUpgraded (SaasCore/Events; no listeners yet — notification session)
- [Done] UsageService (SaasCore/Services) — getUsage(Tenant): vehicles/staff/customers each {current,limit,percentage,approaching} mapped to max_vehicles/max_staff_users/max_customers (mirrors the enforcement counts exactly, so bars never disagree with a PlanLimitExceededException) + storage {files} = count of Agreement.pdf_path + Invoice.pdf_path + ReportExport.file_path (informational only, no % — no byte totals stored). getUsagePercentage/isApproachingLimit(>80%) helpers; unlimited (limit<0) → 0% / never approaching. Runs tenant-scoped (billing portal has current_tenant bound)
- [Done] Tenant BillingController@index (SaasCore) — current plan + subscription status + trialDaysRemaining + usage + disabledModules (Plan::MODULE_KEYS − active plan's) + availablePlans (active) + billing history ($tenant->subscriptions()). Renders Billing/Index. BillingPolicy (tenant-admin only; model-less abilities 'viewBilling'/'requestUpgrade' registered as Gate::define delegations in AppServiceProvider — mirrors ManageMechanicPolicy). Gate::forUser(auth('tenant')->user()) on every action
- [Done] Tenant UpgradeRequestController@store + UpgradeRequestRequest (requested_plan_id must be an ACTIVE plan; notes nullable) — creates UpgradeRequest (current_plan_id from active plan), fires UpgradeRequested, back() with "we'll contact you". tenant-admin gated
- [Done] Super admin — AssignPlanService (the ONLY sanctioned plan-assignment path; ONE DB::transaction): cancels every active/trialing sub (+cancelled_at) → creates new active Subscription (tenant_id EXPLICIT — no bound tenant, INSERT skips TenantScope; period end = +1yr/+1mo via addMonthNoOverflow) → tenant plan_id=new + status=active + trial_ends_at=null → fires SubscriptionUpgraded. TenantManagementController gained assignPlan (AssignPlanRequest: plan_id exists + billing_cycle monthly|annual) + recordOfflinePayment (OfflinePaymentRequest: amount cents min:1, method in METHODS, paid_at date; SubscriptionPayment created with explicit tenant_id + recorded_by=auth('superadmin')->id(); guards no-active-subscription). show() now also feeds plans + activePlanId + per-sub payments (eager-loaded withoutGlobalScope — no bound tenant)
- [Done] Super admin UpgradeRequestController — index (pending queue, oldest-first, withoutGlobalScope) / markContacted / complete (CompleteUpgradeRequest billing_cycle → AssignPlanService then status=completed). CRITICAL: UpgradeRequest is tenant-scoped but the panel has NO bound tenant → resolved by explicit {id} + withoutGlobalScope (NOT route-model binding, which would throw TenantNotResolved), same pattern as SubscriptionManagementController::show
- [Done] Routes — tenant.php (auth:tenant): GET billing, POST billing/upgrade-request. superadmin.php: POST tenants/{tenant}/assign-plan + /offline-payment, GET upgrade-requests, POST upgrade-requests/{id}/contacted + /complete (id-bound, not model-bound). 7 routes
- [Done] Vue (design-system UI kit): Pages/Billing/Index.vue (AppLayout — current-plan card + trial badge, vehicles/staff/customers meters [danger bar >80%, "Unlimited" when limit<0], stored-files count, disabled-module chips, available-plan cards, upgrade-request Modal[plan Select + notes]) · Pages/SuperAdmin/UpgradeRequests/Index.vue (DataTable + mark-contacted/complete[cycle Modal]) · Tenants/Show.vue gained Assign-Plan + Record-Payment modals (AUD→cents via useCurrency.transform) + offline-payments table. SuperAdminLayout nav gained "Upgrade requests"; AppLayout sidebar gained "Billing" — shown ONLY when auth.user.role==='tenant_admin' (mirrors BillingPolicy). i18n: billing.* + nav.billing + superadmin.{nav,tenants,upgrade_requests}.* in en.json; common.superadmin.* + common.billing.* flash in lang/en/common.php. No hardcoded strings
- [Verified] `php artisan migrate` (2 tables, plain). `npm run build` green. tests/Feature/BillingTest.php (DatabaseTransactions; Event::fake only the 2 new events so HasTenant model events still fire): tenant_admin views /billing; tenant_staff → 403; admin submits upgrade request (row + current_plan captured + UpgradeRequested); assignPlan swaps sub (old cancelled, new active annual, plan_id updated, status active, trial cleared, SubscriptionUpgraded); offline payment recorded (cents + recorded_by); mark-contacted. 6 tests. **Full Pest suite 90 passed (351 assertions)** — was 84; +6. No regressions
- [ ] NEXT (Session C): Stripe + PayPal subscription checkout from the billing page (self-service upgrade replacing the contact-us flow), webhook-driven SubscriptionPayment records, invoice/receipt emails on payment; UpgradeRequested → super-admin notification; upgrade-request cancel/reject actions + non-pending history view — **Stripe part DONE in Session 26**; PayPal, receipt emails, UpgradeRequested notification, upgrade-request cancel/reject still open

## Session 26 — Stripe Checkout subscription billing (hosted checkout, webhook-driven activation, cancellation; NO PayPal)
- [Decision] Two client-approved deviations from the brief: (1) NO duplicate `stripe_subscription_id` column — the pre-existing `subscriptions.gateway`/`gateway_subscription_id` columns hold the Stripe identity (`gateway='stripe'`); the new migration adds only `stripe_price_id` + `stripe_status`; (2) `PaymentProviderInterface::constructWebhookEvent()` returns `object` (gateway-agnostic contract for the future PayPal implementer), not `\Stripe\Event` — the Stripe implementation still returns one
- [Done] `composer require stripe/stripe-php` (v20.3.0) + `services.stripe` config block (key/secret/webhook_secret; STRIPE_* already in .env.example). Migrations (plain `php artisan migrate` — never fresh): add_stripe_fields_to_plans_table (stripe_product_id + stripe_monthly/annual_price_id, nullable) · add_stripe_customer_id_to_tenants_table (nullable + INDEX — webhooks resolve tenants by it; created lazily on first checkout, reused after) · add_stripe_fields_to_subscriptions_table (stripe_price_id + stripe_status only; gateway_subscription_id was ALREADY indexed by the original create migration — a duplicate `$table->index()` FAILED on pgsql and was removed; pg's transactional DDL rolled the failed migration back cleanly)
- [Done] `PaymentProviderInterface` extended: createCheckoutSession(Tenant, Plan, cycle, successUrl, cancelUrl): string · constructWebhookEvent(payload, signature): object (MUST throw on bad signature) · cancelSubscription(gatewaySubId): bool. Interface → StripePaymentProvider bound in AppServiceProvider::register (becomes a per-tenant factory when PayPal lands)
- [Done] `StripePaymentProvider` (SaasCore/Providers) — the ONLY place `\Stripe\*` is touched. LAZY StripeClient (built on first use via private client(); an unconfigured STRIPE_SECRET is a call-time RuntimeException, NOT a container 500 — method-injection resolves the provider even on requests that fail authorization first). createCheckoutSession: ensures the Stripe Customer (creates once — billing email = oldest tenant_admin — persists tenants.stripe_customer_id), mode=subscription session with client_reference_id=tenant_id AND tenant/plan/cycle metadata (belt-and-braces tenant resolution for the webhook), returns hosted URL. constructWebhookEvent → \Stripe\Webhook::constructEvent (throws SignatureVerificationException). cancelSubscription → cancel_at_period_end=true (NEVER immediate). charge() = stub throw (subscription billing is Checkout-based; customer-invoice charges are a later session). Plus Stripe-ADMIN ops deliberately NOT on the interface (PayPal has no equivalent): createProduct/updateProduct/createPrice/priceAmount/archivePrice
- [Done] `StripeSyncService` + `php artisan stripe:sync-plans` — mirrors every ACTIVE plan to a Stripe Product + monthly/annual recurring Prices, storing ids on the plan row. IDEMPOTENT: product created once then updated; Stripe Prices are IMMUTABLE, so an amount change archives the old Price and creates a replacement; unchanged amounts are no-ops. Skips is_free plans entirely and zero-priced cycles (archiving any stale price). Per-plan try/catch (one bad plan never aborts the batch); command exits non-zero on any failure. Plan gained stripePriceIdFor(cycle)/priceFor(cycle) helpers + stripe fillables. **Run once with live keys before go-live and after any price change**
- [Done] `StripeCheckoutService` — guards (plan active / not free / synced / not already on this exact plan+cycle via Stripe unless canceling — switching plan or cycle IS allowed; activation cancels the old sub) then provider createCheckoutSession. Throws `CheckoutNotAllowedException` (app/Exceptions; carries a TRANSLATED user-displayable message; no render() — the controller catches it). `StripeCheckoutController` (thin): checkout(CheckoutRequest{billing_cycle} , Plan) — 'manageSubscription' gate (NEW BillingPolicy ability, tenant-admin, Gate::define in AppServiceProvider) → `Inertia::location($url)` (external redirect; success URL carries literal `{CHECKOUT_SESSION_ID}` appended RAW — route() would URL-encode the braces); success() renders Billing/CheckoutProcessing — **NEVER activates** (URL is reachable without paying); cancel() → billing index with "checkout cancelled" flash. Stripe API failures: report() + generic flash, never the raw error
- [Done] `StripeWebhookController` (app/Http/Controllers) + POST /stripe/webhook (routes/web.php — NO auth, NO CSRF [bootstrap/app.php `$middleware->validateCsrfTokens(except: ['stripe/webhook'])` — Laravel 11, no VerifyCsrfToken class]; the VERIFIED SIGNATURE is the only auth). Invalid signature/payload → 400 before ANY processing. Every event logged (id+type). After verification ALWAYS 200 — processing failures are report()+logged, not retried into a storm. Delegates to `StripeWebhookService` (runs UNBOUND — every Subscription read withoutGlobalScope; tenant resolved by stripe_customer_id, falling back to client_reference_id):
  - checkout.session.completed → resolve tenant → plan+cycle from session METADATA (set at session creation; avoids an extra Stripe API call and survives a mid-checkout re-sync) → ActivateStripeSubscriptionAction
  - customer.subscription.updated → stripe_status kept current (cancel_at_period_end+active reads as 'canceling'); past_due also demotes LOCAL status (recovery back to active restores it)
  - customer.subscription.deleted → local status=cancelled + cancelled_at; tenant reverts to trial when trial_ends_at is still future, else cancelled; fires SubscriptionCancelled
  - invoice.payment_failed → stripe_status+status past_due
  - NOTIFY-ONCE GUARD: SubscriptionPaymentFailed fires only on the TRANSITION into past_due (Stripe sends both updated AND payment_failed for one failure, then retries — the admin hears about it exactly once)
- [Done] `ActivateStripeSubscriptionAction` — IDEMPOTENT (existing sub with this gateway_subscription_id → returned as-is; Stripe retries webhooks: no double activation, no duplicate payment). Resolves plan+cycle by matching the price id against plans' stored stripe price ids; DB::transaction: delegates to **AssignPlanService** (which gained an optional `$gatewayAttributes` param merged into the Subscription insert — SINGLE sanctioned plan-assignment path preserved; SubscriptionUpgraded fires in there, not twice) with gateway/gateway_subscription_id/stripe_price_id/stripe_status, then creates SubscriptionPayment (method=stripe, reference=checkout session id, amount=plan price for cycle, recorded_by NULL = system/webhook)
- [Done] `CancelStripeSubscriptionAction` + BillingController@cancelSubscription (POST billing/cancel, manageSubscription gate) — guards an active Stripe sub exists, provider cancel_at_period_end, sets stripe_status='canceling' (LOCAL status stays active — access continues until the deleted webhook lands); idempotent when already canceling. Events SubscriptionPaymentFailed + SubscriptionCancelled (SaasCore/Events) → queued admin-email listeners SendSubscriptionPaymentFailed/CancelledNotification (QueuedNotificationListener base — bindTenant/finally-forget; notifyAdmin is email-only) + SubscriptionPaymentFailed/SubscriptionCancelledTemplate; registered in EventServiceProvider
- [Done] Routes (7 total): tenant.php — POST billing/checkout/{plan} ({plan} binds by id; Plan is platform-wide/unscoped) + GET billing/checkout/success + /cancel + POST billing/cancel. web.php — POST stripe/webhook. BillingController@index props extended: subscription gains gateway/stripe_status/can_cancel; each plan gains can_checkout_monthly/annual (paid AND synced)
- [Done] Vue: Billing/Index.vue — per-cycle Subscribe buttons on plan cards (loading "Redirecting to Stripe…" state; disabled for the exact plan+cycle already held via Stripe; router.post → server answers with the external location) · current-plan card shows "Billed via Stripe" + stripe-status chip (billing.stripe_statuses.* labels) + canceling notice + "Cancel subscription" button behind a confirm Modal · annual price now shown alongside monthly. NEW Billing/CheckoutProcessing.vue — success-redirect landing that deliberately does NOT claim activation (webhook does it). i18n: ~20 new billing.* keys in en.json; 10 common.billing.* flash/guard strings in lang/en/common.php. No hardcoded strings
- [Verified] `php artisan migrate` (3 migrations, plain). tests/Feature/StripeBillingTest.php (DatabaseTransactions — never fresh; StripePaymentProvider Mockery-mocked and bound over BOTH the concrete class and PaymentProviderInterface; SDK never hit): staff checkout → 403; admin checkout → redirect to the hosted URL (success/cancel URLs + {CHECKOUT_SESSION_ID} asserted); unsynced plan blocked with error flash + provider never called; already-held plan+cycle blocked; success URL activates NOTHING; invalid webhook signature → 400; checkout.session.completed activates (old sub cancelled, gateway fields + stripe_status set, tenant promoted + trial cleared, SubscriptionPayment cents/method/reference, SubscriptionUpgraded); duplicate webhook idempotent (1 sub, 1 payment); payment_failed → past_due + SubscriptionPaymentFailed exactly ONCE across two events; subscription.deleted → cancelled + tenant demoted + SubscriptionCancelled; cancel endpoint → stripe_status=canceling while status stays active; cancel without a Stripe sub rejected. 12 tests. **Full Pest suite 102 passed (413 assertions)** — was 90; +12. `npm run build` green. `stripe:sync-plans` + 7 routes registered
- [ ] PENDING LIVE VERIFICATION (needs test-mode STRIPE_KEY/SECRET in .env — not yet provided): run stripe:sync-plans against a real test account, full checkout with card 4242 4242 4242 4242, failed-payment flow with 4000 0000 0000 0341, webhook delivery via Stripe CLI
- [ ] LOCAL WEBHOOK TESTING (developer tooling, not app code): `stripe listen --forward-to localhost:8000/stripe/webhook` (Stripe CLI) and put the printed whsec_… into STRIPE_WEBHOOK_SECRET. In production, register https://dvaro.com.au/stripe/webhook in the Stripe dashboard (events: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed) and use that endpoint's signing secret
- [ ] NEXT: PayPal checkout (provider factory per tenant), receipt email on SubscriptionPayment, UpgradeRequested → super-admin notification, upgrade-request cancel/reject, customer-invoice card payments (separate from platform subscription billing)

## Session 27 — Profile settings for all 4 authenticated guards (tenant, customer, mechanic, superadmin; NO new migrations — color_mode already existed from Session 22)
- [Decision] Mechanic identity check on password/PIN change accepts the current password OR the current PIN via Hash::check (client-approved): `mechanics.pin`/`password` are BOTH nullable — a PIN-only mechanic has no password to present, so requiring `current_password` literally would lock them out. The rule lives in ONE place (ProfileFormRequest::currentCredentialRule) and only the Mechanic model carries a pin, so other guards can never verify with one
- [Done] Schema audit confirmed all four tables already carry everything needed (name/email/password/color_mode + role on tenant_users/super_admins; pin/phone on mechanics; NO name on customer_users — the display name lives on the linked Customer): zero migrations, `php artisan migrate` = "Nothing to migrate"
- [Done] Abstract `App\Http\Controllers\ProfileController` (same shape as PortalPasswordResetController): showProfile + updatePassword live in the base; each guard subclass supplies guard()/profileView()/profileProps() and defines updateProfile() with its own typed FormRequest delegating to applyProfileUpdate(). Concrete controllers in their modules: TenantProfileController (SaasCore) · CustomerProfileController (Customer) · MechanicProfileController (Workshop; + updatePin) · SuperAdminProfileController (SuperAdmin). EVERY action authorizes via Gate::forUser(auth($guard)->user())->authorize('manageOwnProfile') — new model-less ProfilePolicy (App\Policies) registered as a Gate::define delegation in AppServiceProvider; the ability is structurally always-true (a profile action only ever touches the authed user's own row) but keeps the load-bearing forUser pattern uniform + provides the future restriction hook
- [Done] Requests (app/Http/Requests/Profile — cross-cutting like the abstract controller): abstract ProfileFormRequest resolves the active guard's user (same 4-guard loop as UserPreferenceController) + currentCredentialRule() = Hash::check against the stored password hash, falling back to the PIN hash when set — NEVER Auth::attempt. UpdatePasswordRequest (current_password + min:8 confirmed; shared verbatim by all 4 guards; 'hashed' cast hashes on save) · UpdatePinRequest (current credential + pin digits_between:4,6 confirmed — NOTE: intentionally tighter than the tenant-admin CRUD's min:4/max:8, per this session's spec) · per-guard profile requests: UpdateTenantProfileRequest (name + email unique per tenant, explicit tenant_id — the unique rule bypasses TenantScope — ignore own id; tenant_users has no softDeletes) · UpdateCustomerProfileRequest (EMAIL ONLY + whereNull(deleted_at)) · UpdateMechanicProfileRequest (name + email + whereNull(deleted_at)) · UpdateSuperAdminProfileRequest (name + email GLOBALLY unique + whereNull(deleted_at))
- [Done] Customer special case: CustomerUser has no name — showProfile passes the linked Customer's name READ-ONLY (customerName prop); updateProfile touches email only; name changes go through the tenant admin's customer management. The linked Customer record (incl. its own email) is deliberately NOT synced — it is the tenant-managed profile/ledger record
- [Done] Routes (13): tenant/customer/mechanic/superadmin each get GET profile + PUT profile + PUT profile/password inside their existing auth group ({guard}.profile[.update/.password]); mechanic additionally PUT profile/pin (mechanic.profile.pin). All behind auth:tenant/customer/mechanic/superadmin.auth respectively
- [Done] Vue (design-system kit): Pages/Profile/{TenantProfile (AppLayout), CustomerProfile (CustomerLayout; read-only name w/ note), MechanicProfile (MechanicLayout; + PIN card, numeric inputmode; current-credential label switches to "Current password or PIN" when a PIN exists via hasPin/hasPassword props), SuperAdminProfile (SuperAdminLayout)}. Shared Components/Profile/{PasswordCard (posts to the guard's password endpoint, resets on success, overridable current-label), AppearanceCard (light/dark/system segmented control on useColorMode.setMode — persists via the existing per-guard preference endpoint; added for discoverability, TopBar toggle unchanged)}
- [Done] Navigation: AppLayout + SuperAdminLayout now pass profileHref to TopBar (the user-menu Profile link, dormant since Session 22, is live); CustomerLayout top nav gained a Profile item; MechanicLayout header gained a UserCircleIcon profile link. i18n: new top-level profile.* namespace in en.json (~28 keys); common.profile.{updated,password_updated,pin_updated} flash in lang/en/common.php; lang/en/profile.php (current_password_incorrect)
- [Verified] `php artisan migrate` = Nothing to migrate (as planned). tests/Feature/ProfileTest.php (DatabaseTransactions — never fresh; real HTTP stack; 12 tests): tenant name/email update; same-tenant email collision rejected while the SAME address on another tenant is accepted (per-tenant uniqueness); own email re-save passes (ignore own id); wrong current password rejected + correct one updates (Hash-verified); new password logs in after logout; mechanic PIN change → new PIN authenticates on the PIN login path; PIN-ONLY mechanic (password null) adds a password by verifying with their PIN + bad credential rejected; PIN format 4–6 digits enforced; customer email-only update (linked Customer untouched) + per-tenant uniqueness; super admin email globally unique + update; all 4 guards' profile GETs redirect guests to the right login. **Full Pest suite 114 passed (472 assertions)** — was 102; +12. `npm run build` green (4 profile pages + 2 shared cards compiled). 13 profile routes registered
- [ ] Possible follow-ups: email-change verification (none of the guards verify email today); notify on password change (Notification session); tenant setting to lock staff self-service email changes (ProfilePolicy is the hook)

## Session 28 — Customer identity documents (licence front/back + proof of address; SENSITIVE, private disk, signed-URL access only)
- [Decision] Classified as SENSITIVE (same role as agreement/invoice PDFs), NOT public like QR/CMS images: stored on `config('filesystems.default')` (local → `storage/app/private`, or private S3), never the `public` disk. Effective per-upload limit = LOWER of the 10MB ceiling and the plan's `max_file_size_mb` (client-confirmed default). Types jpg/jpeg/png/pdf only (no HEIC)
- [Done] Migration `add_document_paths_to_customers_table` (plain `php artisan migrate`): `license_front_path` / `license_back_path` / `proof_of_address_path` — nullable strings holding PATHS, not files
- [Done] Customer model: `DOCUMENT_TYPES` const (type → column; the single source of truth for route constraint/request/action/page); path columns in `$hidden` (never reach Inertia/JSON) and deliberately NOT fillable; `documentStatus()` → `{type: bool}` for the UI
- [Done] `PlanEnforcementService::checkFileSize(bytes)` (hard block → PlanLimitExceededException `max_file_size_mb`, message `common.plan.file_too_large`) + `fileSizeLimitMb()` (null = unlimited; absent/negative key = unlimited, same convention as check())
- [Done] `UploadCustomerDocumentAction` — the ONLY writer of the path columns. Re-validates type, content-sniffed extension (`guessExtension`, not the client filename), 10MB ceiling and plan limit (so a future /api/v1 caller is equally bound). Path `tenants/{tenant_id}/customers/{customer_id}/{type}-{uuid}.{ext}`. Order: store new → forceFill column → delete OLD file (a failed write never loses the existing document; replaced files are never orphaned). Fires new `CustomerDocumentUploaded(customer, type, replaced)` event (no listeners yet)
- [Done] `UploadDocumentRequest` — `file` required|file|mimes:jpg,jpeg,png,pdf|max = min(10240, plan MB×1024) so oversize shows as an inline field error; `type` merged from the route segment and validated `in:` DOCUMENT_TYPES
- [Done] CustomerController `uploadDocument` (Gate::forUser tenant → `update`) + `downloadDocument` (Gate::forUser tenant → `view`; null path → 404; else redirect to `FileUrlService::temporaryUrl` 15-min signed URL — same pattern as Reporting downloadExport). `show()` passes `documents` = documentStatus()
- [Done] routes/tenant.php: POST/GET `customers/{customer}/documents/{type}` (`tenant.customers.documents.upload|download`), `->whereIn('type', DOCUMENT_TYPES)` → unknown type 404s before the controller
- [Done] Customer/Show.vue "Documents" card — three slots (status dot, On file / Not uploaded, View link → auth-checked route in new tab, Upload/Replace button → hidden file input, useForm + forceFormData, progress bar, inline error). i18n `customer.documents.*` (en.json) + `common.customer.document_uploaded` / `common.plan.file_too_large` (lang/en/common.php)
- [Done] Soft-deleting a customer leaves documents in place (no hard deletes); only REPLACING a document deletes the old file. Tenant staff only — not exposed in the customer portal
- [Verified] tests/Feature/CustomerDocumentTest.php — **11 passed (58 assertions)**: upload lands at `tenants/{t}/customers/{c}/license_front-{uuid}.jpg` on the default disk (not public) + column set + event; replace deletes old / keeps exactly 1 file; download → temporary URL for own tenant; missing doc 404; guest redirected on both routes; cross-tenant: tenant-B slug + tenant-A customer id → 404 (TenantScope binding), tenant-B user on tenant-A slug → 403 (CustomerPolicy) on both GET and POST, original file untouched; >10MB rejected; plan `max_file_size_mb=1` rejects 2MB via HTTP (field error) AND via the action (PlanLimitExceededException); bad type 404 / .exe mimes error; paths absent from toArray + Inertia props. REAL-disk end-to-end: file physically under `storage/app/private/tenants/...`, signed URL (`signature=`) serves 200 unauthenticated-but-signed, same URL unsigned → 403 (test cleans its directory)
- [Verified] Full Pest suite: 125 passed, 2 failed — BOTH pre-existing & unrelated (DemoSeederTest expects 7 coastline customers; the dev DB holds an extra manually-created customer "Olivia Martin", id 311, 2026-08-21). Confirmed failing identically with this session's changes stashed. `npm run build` green
- [ ] MANUAL BROWSER VERIFY: upload/replace/view each slot on a real customer; dark mode; phone width
- [ ] Later: `/api/v1` endpoint (action is ready); audit-log listener on CustomerDocumentUploaded; count these files in UsageService storage figure

## Session 29 — Client feedback round 1 (A): fleet expiry columns (#7), max_mechanics plan limit (#10), mechanic number-plate search (#11)
- [Decision] Client feedback round 1 triaged; build order agreed: this session = #7 + #10 + #11; then #6 reminders, #8 public lead form, #9 expenses (one per session); then #3 agreement templates, #4 invoice templates. Client answers recorded: #6 → all tenant staff, EMAIL only, odometer from manual entry + service log + return inspection; #3 → per Australian state, freely editable; #4 → logo/colour/text + 4 ready-made templates; #9 → per tenant (GST still to confirm); #8 → Google reCAPTCHA. Item 5 omitted by mistake
- [Done] #7 Fleet list — Vehicle: `EXPIRY_WARNING_DAYS = 30`, `SORTABLE_DATES` whitelist, `scopeExpiringSoon()` (registration_expiry OR next_service_due within 30 days or past), `expiryState(column)` → overdue / due_soon / ok / null (server-side, app timezone). FleetController@index: `?expiring=1` filter, `?sort=registration_expiry|next_service_due&direction=asc|desc` (whitelisted; `NULLS LAST` both directions; unknown sort ignored), per-row `registration_state` / `service_state`, `expiringCount` + `filters` props. Fleet/Index.vue: two sortable columns (en-AU date + Overdue/Due soon badge, dash when empty; aria-sort), amber "Expiring soon (n)" chip that combines with status tabs. Date-only for now — km-based service lands with #6
- [Done] #10 max_mechanics — added to `Plan::LIMIT_KEYS` (super-admin plan form + StorePlanRequest pick it up automatically; label `superadmin.plans.limit_keys.max_mechanics`). CreateMechanicAction → `PlanEnforcementService::check('max_mechanics', Mechanic::count())` HARD BLOCK; count excludes soft-deleted, INCLUDES deactivated (same rule as staff — deactivate/reactivate can't bypass). UsageService gains a `mechanics` meter; Billing/Index.vue shows 4 meters (sm:2 / lg:4 grid); public PlanCard lists "Up to N mechanics"
- [Done] PlanLimitExceededException::render — CROSS-CUTTING FIX: JSON callers (expectsJson) still get the 403 `PLAN_LIMIT_REACHED` payload; browser/Inertia submissions now redirect BACK withInput + `plan_limit` validation error + `error` flash (toast). Previously EVERY limit hit (vehicles, customers, mechanics) rendered raw JSON in an Inertia error modal. Mechanic/Create.vue shows an inline amber banner with a "View plans" link to billing
- [Done] #11 Plate search — `Workshop/Services/VehicleLookupService`: `normalizePlate()` (uppercase alphanumerics only), `searchByPlate()` (PostgreSQL `UPPER(REGEXP_REPLACE(registration_number,'[^A-Za-z0-9]','','g')) LIKE %needle%`, exact match first, max 10, min 2 chars, TenantScope), `resolveForLog(vehicleId, token)`, `findByToken()`. MechanicPortalController: `searchVehicles` (1 match → redirect to vehicle; else Workshop/Mechanic/VehicleSearch), NEW id-based `showVehicle(Vehicle)` (TenantScope binding → cross-tenant 404), `scanResult` now REDIRECTS token → `mechanic.vehicles.show` (printed QR stickers + post-login intended-token redirect keep working via the chain). Routes (auth:mechanic): GET `vehicles/search` (`mechanic.vehicles.search`), GET `vehicles/{vehicle}` whereNumber (`mechanic.vehicles.show`)
- [Done] FIXED latent gap: service logs could only be created for vehicles WITH a QR token (createLog resolved by token only). CreateServiceLogRequest now takes `vehicle_id` (required_without token) OR legacy `token`; ScanResult.vue posts vehicle_id; createLog redirects to the id-based page. Mechanic Dashboard: plate search box (Components/Workshop/PlateSearch.vue — h-11 touch target, autocapitalize characters, no autocorrect) + job links use vehicle id (previously hidden for vehicles without a QR token). i18n `workshop.plate_search.*`, `fleet.expiring_soon`, `fleet.expiry.*`, `billing.usage_mechanics`, `public.pricing.limits.mechanics`, `common.view_plans`
- [Verified] No migrations (`php artisan migrate` → nothing to migrate). New tests: FleetExpiryColumnsTest (5) · MechanicPlanLimitTest (6) · MechanicPlateSearchTest (9) — all green; WorkshopTest updated (log-create now redirects to `/vehicles/{id}`). Full suite **145 passed, 2 failed** — the same 2 pre-existing DemoSeederTest failures (extra manually-created "Olivia Martin" customer in the dev DB; see Session 28). `npm run build` green
- [ ] MANUAL BROWSER VERIFY: fleet list sort/filter + badges (light/dark, phone width); plan-limit banner on Create Mechanic; plate search on a phone in the mechanic portal
- [Done in Session 30] Fleet/Index.vue + Show.vue daily_rate now formatAUD (was raw cents)

## Session 30 — Client feedback round 1 (B): #6 fleet reminders — odometer tracking, service by months OR km, daily digest to all staff
- [Decision] Client answers applied: recipients = ALL tenant staff (every TenantUser: admin/staff/accounts; mechanics excluded), channel = EMAIL only, odometer from staff entry + mechanic service log + return inspection. Defaults (user-approved): lead time 30 days / 1,000 km (tenant-configurable), ONE digest email per staff member per day, only service logs flagged "scheduled service" reset the schedule, insurance moved to the new engine alongside rego
- [Decision] RETURN INSPECTION NOT WIRED — the Rental module is empty scaffolding (no return/end-rental flow exists). `OdometerReading::SOURCE_RETURN_INSPECTION` is reserved; the future return-inspection action must call `RecordOdometerReadingAction` with that source. Listed as outstanding below
- [Deviation from plan] Idempotency uses a dedicated `fleet_reminders` ledger table instead of a `dedupe_key` column on notification_logs: one digest email bundles many vehicle items, so a per-email log row can't express "which items were reminded"
- [Done] Migrations (plain `php artisan migrate`): vehicles += current_odometer, odometer_updated_at, service_interval_months, service_interval_km, last_service_odometer, next_service_km (all nullable) · `odometer_readings` (tenantId; vehicle_id restrictOnDelete; reading; source manual|service_log|return_inspection; loose recorded_by_type/_id; service_log_id nullOnDelete; recorded_at; index tenant+vehicle+recorded_at) · service_logs += is_scheduled_service, odometer_ignored (bool, default false) · `fleet_reminders` (tenantId; vehicle_id cascade; kind registration|insurance|service_date|service_km; stage due_soon|overdue; due_key; recipient_count; sent_at; UNIQUE vehicle+kind+stage+due_key)
- [Done] Fleet: `OdometerReading` model — APPEND-ONLY (updating/deleting hooks throw LogicException). `RecordOdometerReadingAction` — the ONE path for every source; lockForUpdate on the vehicle; reading < current → ValidationException(`reading`, common.fleet.odometer_backwards); equal accepted; appends row, moves current_odometer forward (not fillable — forceFill), fires `OdometerReadingRecorded`. `ResetServiceScheduleAction` (last_service_date/odometer → applyServiceSchedule). Vehicle: `applyServiceSchedule()` (next_service_due = last_service_date + months [addMonthsNoOverflow] when both set, else manual date kept; next_service_km = last_service_odometer + km), `reminderLeadDays()/reminderLeadKm()` (tenant settings, defaults DEFAULT_REMINDER_DAYS/KM — replaces Session 29's EXPIRY_WARNING_DAYS so list, filter and reminders share ONE window), `scopeExpiringSoon($dates)` now also matches km (current_odometer + lead >= next_service_km), `serviceKmState()`, `serviceState()` (more urgent of date vs km)
- [Done] Vehicle create/edit: interval months (1–120) / km (100–1,000,000) + last-service odometer; create also takes a starting odometer (recorded as the first reading inside the create transaction, actor = tenant user); last_service_odometer ≤ current (create: lte when given; edit: max = vehicle's current). Edit never touches the odometer. Create/Update actions call applyServiceSchedule. Edit.vue now slices ISO date casts to YYYY-MM-DD (the date inputs previously received full timestamps). Next-service-date input is disabled with a hint when it is auto-derived
- [Done] FleetController: POST `fleet/{vehicle}/odometer` (`tenant.fleet.odometer`, Gate update, RecordOdometerRequest) · show() passes `odometerReadings` (latest 10) + `serviceState`. Fleet/Show.vue: "Service schedule" card (current odometer, interval "Every N months or every N km", last + next service with km to go / overdue, urgency badge), Record-reading form, odometer history with source badges; daily rate now formatAUD, dates en-AU; last/next service rows moved into the card
- [Done] Workshop: CreateServiceLogRequest/DTO + `is_scheduled_service`; CreateServiceLogAction records the mechanic's odometer (source service_log, actor mechanic, service_log_id) — a LOWER reading never blocks the mechanic: it stays on the log with `odometer_ignored = true` and is not recorded. ChangeServiceLogStatusAction: on the transition INTO completed, a scheduled-service log resets the schedule (ignored odometer → falls back to current). ScanResult.vue: "This was a scheduled service" checkbox (h-5 touch target + hint), per-log odometer line + "not recorded" note + Scheduled badge
- [Done] Reminders — `FleetReminderService` (Notification): per tenant (bound; per-tenant try/catch; never throws) → gated by notify_email_enabled AND fleet_reminders_enabled → candidates from `expiringSoon([rego, insurance, next_service_due])`: registration / insurance / service_date (due_soon within lead, overdue strictly past) + service_km (due_soon within km lead, overdue at/after next_service_km) → minus items already in fleet_reminders (same due_key) → ONE `FleetReminderDigestTemplate` email (inline-styled table: plate linked to the vehicle page, what, due, Overdue/Due soon badge) per TenantUser with an email → ledger rows written only if ≥1 email delivered (total provider failure retries next run). A renewed date / reset schedule = new due_key = re-armed. New command `notifications:send-fleet-reminders` scheduled dailyAt 07:00 Australia/Sydney withoutOverlapping. `ExpiryReminderService` now sends AGREEMENT-END reminders only (vehicle part removed — would double-notify); its command description updated
- [Done] Settings: UpdateNotificationSettingsRequest + controller + Notification/Settings.vue "Fleet reminders" card — enable toggle, days ahead (1–180), km ahead (100–20,000), warning when email is switched off. Settings read with defaults (no onboarding change needed)
- [Done] Fleet list (continues #7): Next Service shows "N km to go / N km overdue" under the date; badge = serviceState(); "Expiring soon" includes km-due vehicles; daily rate now formatAUD (fixes the Session 29 raw-cents note). i18n: fleet.fields.{service_interval_months,service_interval_km,current_odometer,last_service_odometer}, fleet.schedule.*, fleet.odometer.*, workshop.{fields.is_scheduled_service,scheduled_service_hint,scheduled_badge,odometer_ignored}, notifications.fleet.*; backend common.fleet.{odometer_recorded,odometer_backwards,odometer_negative}
- [Verified] tests/Feature/FleetServiceRemindersTest.php — **13 passed (71 assertions)**: staff reading appended + backwards rejected (state unchanged); history append-only (update throws); create records starting odometer + derives next date (+6 months) and km; last-service > current rejected; service-log odometer recorded w/ mechanic actor, lower one flagged `odometer_ignored` + not recorded; repair completion leaves schedule, scheduled-service completion resets date + km; due-soon digest → 3 staff (admin/staff/accounts) once, same-day rerun silent, overdue once after the date passes; km due-soon (custom 500 km lead) then overdue once; renewed date re-arms; disabled reminders / email-off send nothing; full cron sweep keeps tenants isolated (each digest only lists its own plates, own recipient); fleet list `?expiring=1` includes km-overdue vehicle; admin saves settings and lead times take effect. Full suite **158 passed, 2 failed** — the same 2 pre-existing DemoSeederTest failures (dev-DB "Olivia Martin" record). `npm run build` green. `schedule:list` shows both reminder commands
- [ ] Return inspection → `RecordOdometerReadingAction(..., SOURCE_RETURN_INSPECTION)` when the Rental module is built
- [ ] MANUAL BROWSER VERIFY: vehicle create/edit schedule fields; Show "Service schedule" card + record reading (incl. backwards error); mechanic scheduled-service checkbox on a phone; settings card; a real digest email via a configured provider

## Session 31 — Client feedback round 1 (C): #8 public lead capture form (share link, QR, website embed, Google reCAPTCHA)
- [Decision] User-approved: reCAPTCHA **v2 checkbox** (deterministic, reliable in iframes); **platform-wide keys** in .env (`RECAPTCHA_SITE_KEY` / `RECAPTCHA_SECRET_KEY`, config `services.recaptcha`); embed domain allow-list OPTIONAL (empty = any site). Fields: name/phone/email REQUIRED + preferred start date, duration, message (licence/emergency stay on the detailed per-lead intake link)
- [Decision] Cross-site iframe = no DVARO cookies (SameSite) → the public POST is **CSRF-exempt** (`lead/*` in bootstrap/app.php) and **session-free**: validation errors are rendered straight back as page props (422), success renders directly — never redirect()->withErrors(). Protection instead: reCAPTCHA + honeypot (`company_website`) + encrypted start token (≥3 s, ≤24 h) + `throttle:lead-form` (10/hour per IP per form, 200/day per form; over-limit → friendly Unavailable page, 429)
- [Found + fixed] **No anti-clickjacking headers existed** — every page (login, admin) could be framed by any site. New `SecurityHeaders` middleware (web group): `Content-Security-Policy: frame-ancestors 'self'` + `X-Frame-Options: SAMEORIGIN` on every response that doesn't set its own CSP. Only the embed route sets its own (`frame-ancestors *` or `'self' <allowed origins>`, no XFO). Framing policy only — no script/style CSP added
- [Found + fixed] **Per-lead intake 500**: IntakeFormRequest allows a blank email but leads.email is NOT NULL → blank email nulled the column (SQLSTATE 23502, confirmed by probe). IntakeFormController::submit now drops a blank email (keeps what staff captured). Regression test added
- [Done] Migrations (plain `php artisan migrate`): tenants.lead_form_token (64, nullable, UNIQUE — system-written only) · leads += source (default 'link'), referrer_url (2048), submitted_ip (45), captcha_status (nullable) + index (tenant_id, source). Lead: SOURCE_LINK/PUBLIC_FORM/EMBED + SOURCES, CAPTCHA_PASSED/UNVERIFIED
- [Done] Captcha provider pattern: `App\Contracts\CaptchaVerifierInterface` (verify() never throws → passed | failed | unavailable; siteKey()) · `Services\Captcha\GoogleRecaptchaVerifier` (siteverify via Http, 5 s timeout; blank token → failed; non-2xx / connection error → unavailable) · `NullCaptchaVerifier` (no keys → unavailable). Bound in AppServiceProvider on whether the secret is set. Unavailable ⇒ lead ACCEPTED + `captcha_status=unverified` (a Google outage never loses a genuine enquiry); failed ⇒ rejected
- [Done] `CRM\Services\LeadFormService` — resolvePublic (slug + hash_equals token; disabled / suspended / cancelled → null), ensureToken / regenerate (Str::random(40); old links/QR/embeds die), settings (tenant.settings lead_form_enabled / _intro / _allowed_domains), updateSettings (domains normalised to origins, max 10), normalizeOrigin, publicUrl / embedUrl, embedSnippet (iframe `data-src` + script: appends `?ref=host page`, resizes from postMessage — origin derived from the iframe's own src + `e.source` check, so a mis-set APP_URL can't break it), frameAncestors, issueStartToken / isHumanPaced (Crypt), qrSvg. `LeadFormSubmissionService` — validate → honeypot (bot gets normal thanks, nothing saved) → timing → captcha → `CaptureLeadAction` (status new, submitted_at, token, source public_form|embed, referrer only for embeds + http(s)-only, IP; fires LeadCreated + LeadSubmitted → existing admin email)
- [Done] Public routes (web.php, no auth/tenant middleware): GET `lead/{slug}/{token}` (crm.lead-form.show), GET `…/embed` (crm.lead-form.embed), POST `lead/{slug}/{token}` (crm.lead-form.submit, throttle:lead-form). `PublicLeadFormController` binds the tenant only for the write. Vue: `Layouts/LeadFormLayout.vue` (standalone card w/ tenant name + "Powered by DVARO"; embedded = chromeless, transparent, posts CONTENT height via ResizeObserver) · `CRM/PublicLeadForm.vue` (reCAPTCHA v2 explicit render, theme follows dark mode, reset on error; honeypot off-screen + aria-hidden; submit disabled until the box is ticked when a key exists) · `CRM/PublicLeadFormSuccess.vue` · `CRM/PublicLeadFormUnavailable.vue` (unavailable / rate_limited)
- [Done] Tenant side — `LeadFormController` + routes BEFORE `Route::resource('leads')` (so "form" never binds as {lead}): GET `leads/form` (viewAny), PUT `leads/form` + POST `leads/form/regenerate` (tenant_admin only), POST `leads/form/send` (create; `SendLeadFormLinkJob` on queue 'notifications' — email, or SMS only when the tenant's SMS is enabled; link resolved at send time; `LeadFormLinkTemplate`), GET `leads/form/qr` (SVG download, generated on the fly). `CRM/LeadForm.vue`: live/off + 30-day submission count, captcha-not-configured warning, share link + copy, open, QR, send-link modal (email/SMS toggle), embed code + copy, settings (admin; intro, allowed websites add/remove), regenerate with confirm modal. Leads list: "Lead form" button, Source column (badge) + source filter (`?source=`), "Unverified" badge. i18n `crm.lead_form.*`, `crm.public_form.*`, `crm.sources.*`; backend `common.crm.lead_form_*`. `.env.example` + RECAPTCHA_* keys
- [Verified] tests/Feature/PublicLeadFormTest.php — **22 passed (198 assertions)**: valid token renders / wrong token 404 Unavailable; disabled form + suspended tenant unavailable (GET + POST); regenerate kills old link; another tenant's token never opens this form; valid submit → submitted lead (public_form, passed, created_by null) + LeadSubmitted; embed source + safe referrer (javascript: dropped); validation errors rendered back (422, no session); captcha failed rejected; captcha unavailable accepted + unverified; honeypot → thanks, nothing saved; too-fast / forged / missing start token rejected; 11th POST from one IP → 429 rate_limited page; normal pages carry frame-ancestors 'self' + SAMEORIGIN; embed `*` or allow-list, no XFO; staff can view / only admin saves settings (domains normalised; junk rejected) + regenerates; send-link queued on 'notifications', SMS rejected when SMS off; job emails the current link (NotificationLog); QR is SVG; leads list source filter; Google verifier maps success/fail/null/500/connection-error; per-lead blank-email regression. Full suite **180 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [Verified] LIVE (php artisan serve + in-app browser): public page renders; host page on `127.0.0.1:8000` (a different SITE from `localhost:8000`) embedding the snippet → iframe allowed by CSP and auto-resized 760 → 538 px via postMessage; a cookie-less curl POST (real CSRF middleware, not the test bypass) → 200 PublicLeadFormSuccess and the lead stored as source=embed with the host-page referrer + unverified (no local keys); a junk POST → 422, never 419. Smoke lead + temp host page removed afterwards. Found + fixed during this: snippet originally trusted APP_URL for the message origin (dev APP_URL lacks the port → resize silently ignored) and measured documentElement.scrollHeight (can grow, never shrink)
- [Note] `.claude/launch.json` added (php artisan serve :8000) for in-app browser previews — dev tooling, commit or ignore as preferred
- [ ] DEPLOY: set RECAPTCHA_SITE_KEY / RECAPTCHA_SECRET_KEY (Google reCAPTCHA v2 checkbox; add dvaro.com.au + demo.dvaro.com.au as domains). Without them every public lead is flagged Unverified
- [ ] MANUAL BROWSER VERIFY: CRM → Lead form page (copy, QR, send-link modal, settings, regenerate) in light/dark + phone width; reCAPTCHA widget with real keys inside an iframe on a real external site

## Session 32 — Client feedback round 1 (D): #9 Expenses module (Finance) — GST, business-level ledger, receipts, reports, profit per vehicle
- [Decision] User-approved defaults: (a) GST — amounts entered GST-INCLUSIVE; "Includes GST" ticked by default → GST = 1/11 of the total (rounded to the cent), overridable for mixed receipts; unticked → GST 0; totals shown incl. GST, ex GST and GST paid (BAS). (b) Ledger gains BUSINESS-LEVEL entries (customer_id NULL) for TYPE_EXPENSE only. (c) Staff may record expenses. Per company (tenant) only. Out of scope: recurring expenses, bank-feed import
- [Found] `ledger_entries.customer_id` was NOT NULL — the ledger was customer-only, so the pre-existing TYPE_EXPENSE could never be written. Audited every ledger read (CustomerController balances, Customer::hasOutstandingBalance, LedgerService::getBalance): all filter by customer_id, so NULL-customer rows can't reach any balance (proved by test)
- [Done] Migrations (plain `php artisan migrate`): `expense_categories` (tenantId; name; system_key daily|government|utilities|NULL; is_hidden; sort_order; UNIQUE tenant+name, tenant+system_key) + BACKFILL of the 3 defaults for every existing tenant (raw query builder — no bound tenant in migrations; 8 tenants → 24 rows) · `expenses` (tenantId; category restrictOnDelete; vehicle nullable nullOnDelete; expense_date; description; supplier; amount_total / gst_amount / amount_ex_gst cents; includes_gst; payment_method cash|card|bank_transfer|other; receipt_path; notes; created_by → tenant_users; voided_at / voided_by / void_reason; indexes tenant+date, +category, +vehicle) · `ledger_entries.customer_id` → nullable + DB CHECK `ledger_entries_customer_required` (customer_id IS NOT NULL OR type = 'expense'); FK kept
- [Done] Ledger: `LedgerEntry::BUSINESS_TYPES = [expense]`; `LedgerService::appendBusiness()` (rejects customer types; POSITIVE = spent, NEGATIVE = reversal). Still append-only — edits/voids never touch existing rows
- [Done] Models `ExpenseCategory` (DEFAULTS, visible/ordered scopes; hidden never deleted) + `Expense` (HasTenant; PAYMENT_METHODS; receipt_path $hidden + `has_receipt` appended; scopeActive excludes voided; receipt/void cols not fillable). `ExpenseCategoryService` (ensureDefaults — idempotent, works unbound: onboarding + lazily on the Expenses page; create/update with case-insensitive uniqueness). `TenantOnboardingService` seeds defaults inside the onboarding transaction
- [Done] Actions (the ONLY write paths): `RecordExpenseAction` (plan file-size hard block BEFORE writes → txn: expense + ledger +total + receipt; stored file removed if txn fails) · `UpdateExpenseAction` (voided → locked; total changed → ledger −old then +new, otherwise no ledger rows; receipt replace deletes old file after commit; remove_receipt) · `VoidExpenseAction` (reason required; ledger −total; row + receipt kept). Events ExpenseRecorded / ExpenseUpdated / ExpenseVoided → sync `ExpenseReportCacheListener` busts the expenses + revenue_by_vehicle report caches. `ExpenseDTO` holds the GST maths (`standardGst`, override clamp)
- [Done] `ExpensePolicy` (registered in AppServiceProvider): admin + accounts = everything; staff = record + view, edit ONLY own on the SAME day, never void / manage categories; voided → no edit. Receipts via `ExpenseReceiptStorage` — default (private) disk, `tenants/{t}/expenses/{id}/receipt-{uuid}.{ext}`, jpg/png/pdf, max = min(10MB, plan max_file_size_mb), download only via auth-checked route → FileUrlService 15-min signed URL
- [Done] Routes (tenant.php, auth:tenant): GET/POST `expenses`, PUT `expenses/{expense}` (multipart via POST + _method), POST `expenses/{expense}/void`, GET `expenses/{expense}/receipt`, POST `expense-categories`, PUT `expense-categories/{category}`; GET `reports/expenses`. `ExpenseRequest` (category + vehicle `exists` constrained to the CURRENT tenant; date ≤ today; amount_total cents ≥ 1; gst_amount ≤ total)
- [Done] UI — sidebar "Expenses" (after Invoices). `Finance/Expenses/Index.vue`: stat cards (this month, FY to date "FY26/27", GST paid FY); filters (presets This month / Last month / current FY, from–to, category, vehicle, show voided); spend-by-category bars; list (date, description + supplier · method, category, vehicle rego, total + GST line, receipt clip, edit/void by per-row policy flags; voided rows dimmed + struck + reason); slide-over add/edit (GST auto-fills from the total, "reset to 1/11" after a manual override, includes-GST toggle, segmented payment method, optional vehicle, receipt upload w/ progress + current/remove); void dialog (reason); categories dialog (inline rename, hide/show, add; Default badge). Fleet/Show: "Expenses" section (latest 5 + this-FY total + "All" → filtered list). Reporting: "Expenses" tab/page (totals, by category, by month bars) with PDF + Excel export (ExportService TYPES + tabulate + `reports/expenses.blade.php`; job buildData); Revenue page by-vehicle table + PDF gain Expenses / Workshop / **Profit** columns
- [Done] `ReportingService::revenueByVehicle` now returns expenses, maintenance (completed service_logs.total_cost by completed_at) and profit = revenue − expenses − maintenance; includes vehicles with costs but no revenue; sorted by PROFIT (CLAUDE.md "most profitable vehicles"). `ReportingService::expenses()` + `ReportCacheService::expenses()` (TYPE_EXPENSES). `ExpenseReportService` (summary totals / by_category / by_month on expense_date, active only; totalsByVehicle). i18n `expenses.*`, `nav.expenses`, `reporting.nav.expenses`, `reporting.revenue.{expenses,maintenance,profit,profit_hint}`; backend `common.expenses.*`
- [Verified] tests/Feature/ExpensesTest.php — **17 passed (146 assertions)**: record → GST 1/11 + one NULL-customer ledger entry + event; GST override / no-GST / GST > total rejected; description-only edit writes no ledger rows, total edit → [+11000, −11000, +22000] with the original row byte-identical; void needs a reason, reverses, drops out of totals, then locks edit + re-void; customer balance / hasOutstandingBalance / customers list unchanged by a $999 expense; DB CHECK rejects a customer-less payment row; appendBusiness rejects customer types; staff: record ✓, edit own same day ✓, other's ✗, next day ✗, void ✗, categories ✗ (+ row flags false); accounts: edit anyone's back-dated, categories add (case-insensitive dup rejected) / rename+hide default (system_key kept); receipt path/private/signed redirect/hidden attr, replace deletes old, remove clears, none orphaned; plan 1MB limit rejects 2MB with nothing written; /register gives the 3 ordered defaults; ensureDefaults idempotent; FY26/27 boundary (30 Jun excluded, 1 Jul included), GST sum, by_month + by_category order; profit per vehicle −15,000 (no revenue, $100 expense, $50 workshop) + vehicle page section; Expenses report page + export queued on 'exports' + real xlsx + %PDF files; tenant B can't list / edit / void / fetch receipt (404) or use A's category. Full suite **197 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [ ] MANUAL BROWSER VERIFY (needs a tenant login — not done by the agent): Expenses page (slide-over GST auto-fill + override + reset, receipt upload, void, categories dialog) in light/dark + phone width; vehicle page Expenses section; Reports → Expenses + Revenue profit columns; PDF/Excel exports
- [ ] Later: recurring expenses (e.g. monthly utilities), bank-feed import, expense approval flow if the client wants one

## Session 33 — Client feedback round 1 (E): #3 agreement terms templates (per state + type, freely editable, frozen onto each agreement)
- [Decision] User-approved: install TipTap (`@tiptap/vue-3`, `@tiptap/starter-kit`, `@tiptap/extension-underline`) + `symfony/html-sanitizer` (pure PHP, VPS-safe); a NEW VERSION keeps the wording the customer originally agreed to (re-filled with the new details), not the template's latest text; ship clearly-marked SAMPLE platform defaults for the client's lawyer to replace
- [Found] Agreements had NO terms at all (the PDF was customer/vehicle/rate/signature only) — this session introduces legal text, so the hard constraint is CLAUDE.md's immutability: wording is FROZEN per agreement at creation. Also: the only existing HTML editing (super-admin CMS) stores raw HTML from a textarea and renders it unsanitised — fine for platform staff, not for 100+ rental companies whose text reaches their customers
- [Done] Migrations (plain `php artisan migrate`): `agreement_templates` (HYBRID tenant_id — NULLABLE: set = a company's own, NULL = PLATFORM DEFAULT; name; agreement_type nullable; state (8 AU states) nullable; body_html; revision; is_active; indexes) · agreements += state, agreement_template_id (nullOnDelete), template_revision, terms_html · agreements += terms_source (the template body as it read at creation, merge fields unresolved — lets a new version reuse the exact wording after the template is edited)
- [Done] `AgreementTemplate` model — HasTenant (so tenant rows are scope-protected) + documented scope-free readers `platformDefaults()` (tenant_id NULL only) and `visibleTo($tenantId)` (own + defaults, tenant_id constrained explicitly), because TenantScope hides NULL-tenant rows. `AgreementTermsSanitizer` (symfony) — allow-list p/br/strong/b/em/i/u/ul/ol/li/h2/h3; script/style/iframe/object/embed/template/head/noscript dropped WITH content; div/span/table/a/h1/h4-6/etc BLOCKED (tag removed, text kept) so terms pasted from Word aren't silently emptied — Symfony's default drops unknown elements with their contents. `AgreementTermsRenderer` — 18 merge fields + `used()/unknown()/render()` (values HTML-ESCAPED on substitution), `valuesFor(Agreement)`, `sampleValues()` for the preview
- [Done] `AgreementTemplateService` — `resolveFor(tenant, type, state)` cascade (own · type · state → own · type → own · state → own → same four over platform defaults; ACTIVE only), `selectableFor()`, `save()` (sanitise → reject empty / unknown merge field → bump revision ONLY when the wording changes), `copyToTenant()` (platform defaults only). GUARD: HasTenant auto-fills tenant_id from the bound tenant, which would quietly turn a platform default into that tenant's template — `save()` forces tenant_id back to NULL for platform rows (caught by the tests)
- [Done] `AgreementService::create()` now FREEZES the terms after insert: resolve template (explicit choice honoured, validated visible-to-tenant) → render merge fields → store terms_html + terms_source + template id/revision. `createNewVersion()` re-renders the PARENT's terms_source with the new version's details (same template revision); only a pre-templates parent falls back to resolving. No template configured → no terms, agreement still created
- [Done] Tenant UI (Settings → Agreement templates, `agreements/templates` registered BEFORE the agreements resource): list own templates (revision + scope), platform defaults read-only with "Copy and edit", archive/restore, default state (tenant settings `default_state`). `AgreementTemplatePolicy`: everyone reads, tenant_admin writes, platform defaults never editable in place. Super admin → "Agreement terms" (platform defaults only; tenant-owned id 404s). Shared Vue: `Components/Agreement/TermsEditor.vue` (TipTap: H2/H3, bold/italic/underline, bullet + numbered lists, undo/redo, merge-field picker, preview with sample values escaped exactly as the server does) + `Components/Agreement/TemplateManager.vue` (used by both pages)
- [Done] Agreement create form: state (pre-filled from the company default) + terms-template dropdown ("Choose automatically" = cascade). Agreement detail + CUSTOMER PORTAL show the frozen terms; the PDF gains a "Terms and conditions" section (dompdf-safe styles). No typography plugin exists, so `.terms-content` styles were added to app.css (the CMS About page's `prose` classes were always dead — left alone, noted below)
- [Done] `AgreementTemplateSeeder` — 3 SAMPLE platform defaults (private/delivery/rideshare), each opening with "SAMPLE TERMS — NOT LEGAL ADVICE. Replace this wording…", exercising the merge fields. Idempotent, run on demand. i18n `agreement_template.*`, `agreement.terms*`, `superadmin.nav.agreement_templates`, `customer.portal.terms`; backend `common.agreement_template.*`
- [Verified] tests/Feature/AgreementTemplatesTest.php — **16 passed (135 assertions)**: save strips script/onclick/links/divs but keeps every word (incl. table cell text); unknown merge field + empty terms rejected; a customer named `<script>…` is escaped into the frozen terms; full cascade incl. archived-skipped and per-state fallback; explicit template choice wins; company with none inherits the platform default; nothing available → agreement without terms; EDITING A TEMPLATE NEVER CHANGES AN EXISTING AGREEMENT (revision 1→2, agreement untouched); a new version keeps the original wording with the new vehicle even after the template is rewritten; terms reach the PDF (Blade render) + agreement page; staff read-only / admin writes / default-state 403s; platform defaults 403 on edit, copy works and the original stays untouched; cross-tenant 404s; super admin manages platform defaults only. setUp() parks the seeded samples so dev-DB data can't skew the cascade. Full suite **213 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [Note] `composer audit` reports 33 advisories across 6 PRE-EXISTING packages (dompdf ×6, guzzle ×4, others) — unrelated to this session; worth a dedicated `composer update` pass before production
- [ ] DEPLOY: `php artisan db:seed --class=AgreementTemplateSeeder` (idempotent), then replace the SAMPLE wording via Super Admin → Agreement terms with the client's lawyer-reviewed text BEFORE any real agreement is signed
- [ ] MANUAL BROWSER VERIFY: the editor (toolbar, merge-field picker, preview, paste from Word) in light/dark + phone width; copy-and-edit a platform default; create an agreement and check the PDF's terms section; the customer portal view
- [ ] Noticed, out of scope: Pages/Public/About.vue uses `prose` classes but @tailwindcss/typography is NOT installed, so CMS rich text renders unstyled

## Session 34 — Settings & customization (all 8 sections: company profile, staff, finance, regional, integrations, notifications matrix, personal preferences, audit trail)
- [Decision] User-approved: build ALL proposed sections; keep the FOUR FIXED roles (no custom role builder); apply the company timezone EVERYWHERE (display, documents and scheduling), not only reports
- [Found] A rental company could not add a second user at all — there was no staff management anywhere, and every settings value was read with scattered `$tenant->settings['x'] ?? default` fallbacks that had already drifted between readers. No change to any setting was recorded
- [Found + fixed] `AuditLog` threw `AppendOnlyException` but imported `LedgerImmutableException` — update()/delete() on an audit row would have fatalled with "class not found" instead of refusing cleanly. Caught by the new test
- [Found + fixed] The fleet digest was scheduled `dailyAt(07:00)` Australia/Sydney for EVERY tenant, so a Perth company was emailed at 04:00 local. Now scheduled `->hourly()` with `--hour=7`: each tenant is swept only when the clock in ITS OWN timezone reads 7am
- [Done] Migrations (plain `php artisan migrate`): `audit_logs` (tenantId; actor_type/_id/_label across all 4 guards; action; subject_type/_id/_label; old_values/new_values json; ip; user_agent; created_at only) · tenant_users += is_active, last_login_at, preferences json · `tenant_user_invitations` (name, email, role, token, invited_by, expires_at, accepted_at) · preferences json on customer_users / mechanics / super_admins
- [Done] `TenantSettingsService` — THE source of truth: every default in one `DEFAULTS` const, `all()/get()`, and `update($tenant, $changes, $section)` which merges (never clobbers unrelated keys) and writes ONE audit entry with only the keys that actually changed. `AuditLogger` — the one way to write an audit row: resolves the actor across all four guards, redacts password/token/secret/api_key, diffs old vs new, and NEVER throws (a failed audit write must not fail the user's action). `AuditLog` is append-only (update/delete throw `AppendOnlyException`, new shared exception with modify()/remove())
- [Done] Staff management (the gap): `InviteStaffAction` (plan seat limit counts users + PENDING invitations), `AcceptStaffInvitationAction` (consumes the invitation in the SAME transaction as the account), `UpdateStaffAction`. Guardrails: you cannot change your own role, deactivate yourself, or demote/deactivate the LAST active admin. DVARO never emails a password — `SendStaffInvitationJob` (queue 'notifications') sends a single-use link; the invitee sets their own. Deactivated staff are blocked at login by `$credentials['is_active'] = true` — indistinguishable from a wrong password (no account probing). `last_login_at` stamped on every sign-in
- [Done] Settings hub + sections (tenant.php, auth:tenant; public `invitation/{token}` GET/POST throttle:6,1 registered BEFORE the auth group): `settings` (cards by role + recent changes), `settings/activity` (admin-only audit trail), `settings/company` (+ logo POST/DELETE — PUBLIC disk, 2MB, replacing deletes the old file), `settings/staff` (+ invite/update/resend/revoke), `settings/finance`, `settings/regional`, `settings/integrations`. Vue: Settings/{Index,CompanyProfile,Staff,Activity,Finance,Regional,Integrations}.vue + Auth/AcceptStaffInvitation.vue
- [Done] Late fees were UNSETTABLE — `LateFeeService` / `ApplyLateFeeAction` now read through TenantSettingsService and honour `late_fees_enabled`, grace days, fixed-vs-percentage. The settings default grace is 3 days to match the service's long-standing DEFAULT_GRACE_DAYS (behaviour preserved, not silently changed)
- [Done] Timezone end to end: TenantMiddleware shares `tenant.timezone` / `date_format` / `currency`; new `useTenantFormat()` composable (date/dateTime/time/money in the COMPANY's settings — date-ONLY values rendered exactly as stored so a timezone can never shift the day) adopted by Settings Index/Activity/Staff, Fleet Index/Show, Expenses and Billing; `FleetReminderService::sweep(?int $localHour)` + `--hour=` on the command
- [Done] Notification matrix — `NotificationMatrix`: one row per trigger (agreement signed, invoice issued, late fee, payment received, agreement ending, fleet digest, new lead, subscription failed/cancelled) × channel, split by audience (customer vs team). TWO gates: the tenant's master channel switch AND the trigger's own flag. Only DIFFERENCES from the defaults are stored, so an untouched tenant behaves exactly as before. Billing alerts are LOCKED (a company cannot silence its own payment failures); unsupported channels (SMS to staff, who have no phone) are never offered. `QueuedNotificationListener`, `FleetReminderService` and `ExpiryReminderService` all consult it. Settings → Notifications rewritten around the matrix (providers moved to Settings → Integrations); trigger labels live in `lang/en/notifications.php`, never hardcoded
- [Done] Personal preferences — `UserPreferences` (per PERSON, not per company) on all four guards: landing page after sign-in (honoured by the tenant/customer/superadmin login redirects, with a stale value falling back to the dashboard rather than breaking sign-in), rows per page (shared `PaginatesForUser::perPage($fallback)` trait wired into 12 list controllers, each keeping its own number as the fallback), and a personal fleet-digest opt-out that silences ONLY that person. Shared `Components/Profile/PreferencesCard.vue` on all four profile pages. Dark/light stays on its own column + endpoint
- [Verified] tests/Feature/SettingsTest.php **25 passed**, NotificationMatrixTest.php **10 passed**, UserPreferencesTest.php **10 passed**: audit entries record old AND new values (unchanged keys not stored) and cannot be updated or deleted; activity log admin-only; hub cards per role; ABN/brand-colour validation; logo replace deletes the old file and remove leaves nothing behind; invite queues the email and creates NO account until accepted; the accept link works once, expired/revoked links do not; duplicate + existing emails rejected; plan seats count pending invitations; last-admin and self-guardrails; deactivated staff cannot sign in; another company's staff id 404s; late-fee settings change the actual charge and can be switched off; the digest lands at 7am in each tenant's own timezone (Perth first, Sydney three hours later); an untouched tenant keeps pre-matrix behaviour; a channel can be silenced for one trigger only; the master switch still wins; billing alerts cannot be silenced; sanitize drops unknown triggers/channels/locked rows; silencing a trigger stops the real notification end to end; preferences save per person without touching a colleague's, change what a list returns, land you on your chosen page and mute only your own digest. Full suite **258 passed, 2 failed** (same pre-existing DemoSeederTest — the dev DB holds a manually-created customer). `npm run build` green
- [ ] MANUAL BROWSER VERIFY (needs a login — not done by the agent): the settings hub and all seven sections in light/dark + phone width; the invitation email link and accept screen; the notification matrix table (greyed columns when a master switch is off); Profile → Preferences on all four portals
- [ ] Next: client feedback #4 — invoice templates (logo, colour, text + 4 ready-made layouts). The company logo and brand colour built here are what it will consume
## Session 35 — Client feedback round 1 (F): #4 invoice templates (4 ready-made layouts, logo, colour, wording) + the ABN/GST gap
- [Decision] User-approved: four ready-made layouts; logo, accent colour and editable text; text stored as PLAIN TEXT (it prints into a customer-facing PDF, so there is no editing case for markup); the preview renders HTML, never a synchronous PDF (CLAUDE.md). GST/ABN fix included under stated assumptions — rental prices are GST-INCLUSIVE (the convention Expenses already uses) and `gst_registered` defaults to on
- [Found] `resources/views/pdf/invoice.blade.php` was still marked "FUNCTIONAL ONLY, design pass later": no logo, no colour, and the only company detail on it was `$tenant->name` — no ABN, no address, no contact, nowhere to put bank details
- [Found] The PDF was titled **"Tax Invoice"** while showing no ABN and no GST, and `invoices` has no tax column at all. Under ATO rules a tax invoice over $82.50 must show the supplier's ABN and either the GST amount or "Total price includes GST" — DVARO was issuing documents that called themselves tax invoices and were not. Fixed at render time: GST = 1/11 of the GST-INCLUSIVE total, stated as "Total includes GST of $X". NO migration and NO stored amount changes; not registered → the document says "Invoice" and the GST line disappears
- [Done] `InvoiceTemplateService` (Invoice module) — LAYOUTS, LIMITS, DEFAULTS, `resolve($tenant, $forScreen)`, `forUi()`, `sanitize()`, `gstOf()`. Presentation only: it cannot reach an amount, a due date or a ledger entry. sanitize() strips tags and control characters, caps every field, and rejects a non-hex colour or an unknown layout; a junk layout falls back to Classic rather than failing. Settings live under `invoice_template` (+ `gst_registered`) and every save goes through TenantSettingsService, so changing the invoice design lands in the audit trail like everything else
- [Done] Four dompdf-safe layouts under `resources/views/pdf/invoices/` — `classic` (traditional, ruled headings — the default and what existing invoices already looked like), `modern` (full-width accent band), `minimal` (no rules, amount due stated large), `compact` (dense, three-panel header — a month of daily rental lines fits on one page). `pdf/invoice.blade.php` is now the SHELL that prepares the data and picks one; the items table, totals, payments and footer are shared partials, so the four can differ in look and never in figures
- [Done] Logo and colour reuse Settings → Company (brand colour + company logo) with an optional invoice-specific logo override; company details (legal name, trading name, ABN, address, phone, email) finally appear on the document. Tenant wording is escaped on render AND stripped on save, so nothing a company types can inject markup into a customer's PDF
- [Done] Settings → Invoices (`settings/invoice-template`, admin or accounts writes, everyone reads): layout picker with a small wireframe of each drawn in the live accent colour, logo upload/remove, colour picker with "use my brand colour", GST registration, and the five text fields — beside a LIVE PREVIEW iframe rendering the real template with sample data. Hub card added. `POST invoices/{invoice}/pdf` re-queues an older invoice's PDF onto the current template (`InvoicePolicy::regeneratePdf`, admin/accounts; pdf_path is a cached artefact, not a financial record)
- [Found + fixed during a LIVE browser check] (a) No `body { margin: 0 }` — the browser/dompdf default 8px body margin sat on top of the `@page` margin, insetting the Modern accent band and pushing the layout off the right edge. (b) Browsers ignore `@page`, so the settings preview would have sat flush against the iframe edge — added `@media screen { body { padding: … } }`, which dompdf ignores, so the PDF is untouched. (c) The preview showed NO LOGO, because dompdf needs an absolute filesystem path while a browser cannot open one → `resolve($tenant, forScreen: true)` returns a ROOT-RELATIVE url instead, deliberately not `$disk->url()` (APP_URL-derived, drops the port in local dev — the same trap as the Session 31 embed snippet)
- [Verified] LIVE (php artisan serve + in-app browser, viewport 794px ≈ A4): all four layouts rendered through the real Blade with a logo, six line items, a part payment and GST, checked at page width and measured for overflow in the DOM. Real PDFs were produced through dompdf exactly as the job does (`%PDF`, logo embedded from the public disk — dompdf's chroot is the project root, so storage/app/public is readable). Throwaway HTML/PDF files, the preview logo and public/_tmp_preview all removed afterwards
- [Verified] tests/Feature/InvoiceTemplateTest.php — **26 passed (130 assertions)**: all four layouts render the same figures, the ABN and the items; an unconfigured company still gets a working invoice; a junk layout falls back; colour and wording reach the document and newlines survive; the invoice logo beats the company logo, both are optional, a `..` path is refused and a deleted file does not break the invoice; the logo is a FILE PATH for the PDF and a root-relative URL on screen; GST is stated as a share and never added (and the stored total is byte-identical after), rounding to the cent, absent when not registered; sanitize strips markup, collapses blank lines and caps length; a tenant's wording is escaped on render; the screen renders, saves and is AUDITED (`settings.invoices.updated`); junk is rejected; the preview renders the chosen layout and writes NO invoice; logo upload replaces and removes cleanly and saving the form does not drop it; staff read-only, accounts may write; one company's design never reaches another's invoice; regenerating re-queues on the `pdf` queue without touching the money, is refused for staff, and 404s across companies; the job itself writes a real PDF. Full suite **284 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [ ] MANUAL BROWSER VERIFY (needs a login — not done by the agent): Settings → Invoices in light/dark + phone width (layout picker, colour picker, logo upload, live preview iframe); the "Rebuild with current template" link on an invoice; a downloaded PDF on paper
- [ ] ASSUMPTION TO CONFIRM WITH THE CLIENT: rental prices are GST-inclusive (GST = 1/11 of the total) and every tenant is GST-registered by default. If a company prices ex-GST, this needs a real tax column on invoices and a migration — out of scope here and flagged deliberately
- [Done] AGREEMENT PDFs now carry the same letterhead (done straight after, on request). New shared `App\Services\TenantBranding` — logo, accent colour, company details and date format, the three things every DVARO document shows — with `InvoiceTemplateService` refactored to use it instead of its own copies (the Agreement module must not depend on the Invoice module). `pdf/agreement.blade.php` gains the letterhead block, accent headings, `body { margin: 0 }` + `@page`, and the company date format; its CONTENT is untouched, because it is a signed legal document. NO regenerate action for agreements, deliberately: each agreement's PDF is written once at signing and kept, so a company that rebrands later does not retro-change what people already signed. The Blade also renders fine with no branding passed at all, which other tests and older callers rely on (both covered)
## Session 36 — Dashboard, phase 1 (needs attention, fleet snapshot, the week ahead, polling)
- [Decision] User-approved: build the operational half first (attention list, fleet, week), pause for review, then the money row. Role rule: tenant_staff sees operations but NOT revenue or receivables; admin + accounts see money
- [Found] The dashboard was 69 lines and six stat cards, while ReportingService already computed revenue, utilisation, overdue payments, customer growth, maintenance and expenses — none of it surfaced. CLAUDE.md's "dashboard KPIs via polling (30–60s)" was also unimplemented anywhere in the app
- [Done] `DashboardService` (Reporting module) — `needsAttention($includeMoney)`, `fleetSnapshot()`, `thisWeek()`, `seesMoney($user)`. Six attention groups, each carrying what/how-urgent/where-to-click: overdue invoices (urgent, the only group stating money), registration/insurance/service due by DATE or KM (urgent when anything is past), rentals ending within 14 days, submitted-but-unconverted leads, vehicles in accident/suspended, and workshop jobs stuck waiting for parts or re-inspection. Each group caps at 5 rows but reports the TRUE count, so "+N more" links to the full screen. Every figure is a grouped/aggregate query — no collections loaded just to be counted
- [Done] Fleet snapshot: all six CLAUDE.md statuses always present (an empty status reads as zero rather than vanishing), each a clickable segment of a proportional bar linking to the filtered fleet list, plus live utilisation (% of the fleet on the road right now — deliberately NOT the period-average utilisation on the reports page; they answer different questions). Divide-by-zero guarded for a company with no vehicles
- [Done] "The week ahead": rentals starting/ending today and within 7 days, open services, leads waiting
- [Done] ROLE ENFORCED SERVER-SIDE: `TenantDashboardController` computes `seesMoney` once and a staff payload never CONTAINS receivables or the overdue group — unset from the props, not hidden with CSS. `ReportCacheService::dashboardOperational($includeMoney)` caches the two payloads under separate keys, so a staff member can never be served an admin's cached copy
- [Done] POLLING (CLAUDE.md, first implementation): the operational blocks are a plain CLOSURE prop — NOT `Inertia::lazy()`, which is skipped on a full page load and would paint an empty dashboard — so they render on first paint and re-evaluate on a partial reload. The page polls every 60s with `router.reload({ only: ['operational'] })`, pauses while the tab is hidden (`visibilitychange`) and catches up on return, so a forgotten tab stops hitting the VPS. 5-minute cache TTL behind it: fresh enough for a to-do list, cheap enough for the most-hit page in the app
- [Found + fixed] Days-overdue read **"-40 days overdue"** — Carbon's `diffInDays` is signed and the due date is in the past. Caught by the test, fixed with abs()
- [Done] UI rebuilt around the attention panel, with an all-clear empty state ("You're all caught up") rather than a wall of zeros; dates and money through `useTenantFormat` (company timezone + format); manual refresh button with a spin state; i18n `dashboard.attention.*`, `dashboard.fleet.*`, `dashboard.week.*`
- [Verified] tests/Feature/DashboardTest.php — **23 passed (125 assertions)**: a brand-new company sees an empty list; overdue invoices sorted oldest-first, flagged urgent, not-yet-due excluded, and a paid invoice drops off; the most urgent of a vehicle's three dates wins (overdue beats due-soon); service-due-by-km counts as an expiry; agreements ending / leads waiting / off-road vehicles counted correctly (open-ended agreements and never-submitted leads excluded); long lists capped at 5 while the count stays true; the fleet snapshot covers every status at zero and does not divide by zero; the week block counts starting/ending today and this week; STAFF ARE NEVER SENT MONEY (asserted on the absence of the props, not their visibility) while admin and accounts are; one company never counts another's rows; the cache serves the second read and can be busted; staff and admin payloads cache separately; a partial reload returns ONLY `operational`. Full suite **307 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [Verified] Against real seeded data (outback tenant): 6 items — 1 invoice 75 days overdue, 4 vehicles with service due 08/10/2026, 1 lead waiting; fleet 4 vehicles at 25% on the road
- [Done] PHASE 2 — the money block (admin + accounts only): cash received this month vs last with a % change (null, not infinity, on a first month), expenses this month, net, receivables AGED into not-yet-due / up-to-30-days / over-30-days, and a six-month revenue sparkline drawn as hand-written SVG (six points do not justify a charting library on a page that polls itself). Revenue is CASH RECEIVED, not invoiced value — the question an owner actually asks. The block is absent from a staff payload entirely (null, not zeroes), and the old "outstanding balance" KPI tile is suppressed when the block renders so the page never states the same number twice
- [ ] MANUAL BROWSER VERIFY (needs a login — not done by the agent): the dashboard in light/dark + phone width; the fleet bar segments linking through to filtered lists; the 60s poll updating after an invoice is paid in another tab
## Session 37 — Missing-PDF recovery (agreements stuck on "the PDF is being generated")
- [Found] Reported live: an agreement says "The PDF is being generated. Refresh shortly." forever. ROOT CAUSE: PDF generation is queued (CLAUDE.md — dompdf is far too slow to block a request) and with NO QUEUE WORKER running the job is never consumed. The job itself is fine — proved by running it by hand on agreement #40, which produced its PDF in under a second. 48 documents were stranded this way across the local tenants (9 agreements + 39 invoices)
- [Found] There is **no CI/CD at all** — the `.github/workflows/` directory does not exist, despite CLAUDE.md describing a full GitHub Actions pipeline. Pushing moves code to GitHub and nothing else; deployment is manual. ALSO: the live domain dvaro.com.au is served from the **staging** branch, not main (per the client), which inverts the documented branch-to-environment mapping
- [Done] `php artisan pdfs:generate-missing` — re-queues agreements AND invoices whose PDF is absent, with `--tenant=`, `--type=agreements|invoices|all` and `--dry-run`. Treats a recorded path whose FILE has been deleted as missing too (it reads to the user exactly like one that never generated). Runs unbound and binds each tenant; per-tenant failures are logged and skipped. Safe to re-run — it only touches what is genuinely absent
- [Done] "Generate it now" button on the agreement page beside the pending message (`POST agreements/{agreement}/pdf`, `AgreementPolicy::rebuildPdf`, admin/accounts). DELIBERATELY refused (403) when the PDF already exists: an agreement PDF is written once at signing and kept, so a company that rebrands later must never be able to re-render a document someone has already signed. Content could not change either way (the terms are frozen) — appearance would, and that is not ours to alter
- [Verified] tests/Feature/MissingPdfRecoveryTest.php — **10 passed (38 assertions)**: the button appears only when the PDF is missing; rebuilding queues on the pdf queue; an EXISTING pdf is refused and nothing is dispatched; a path pointing at a deleted file CAN be rebuilt; staff refused and another company 404s; the command queues only what is missing, honours --dry-run and --type, fails loudly on an unknown tenant or type, and leaves complete documents alone. Full suite **317 passed, 2 failed** (same pre-existing DemoSeederTest). `npm run build` green
- [Verified] Ran for real on the local database: 48 documents queued, a worker drained them, and --dry-run now reports 0 missing
- [ ] PRODUCTION: confirm a Supervisor queue worker is actually running on the VPS (max 3 processes per CLAUDE.md). Without it EVERY agreement PDF, invoice PDF and email on the live site is in this state. Then run `php artisan pdfs:generate-missing` once to recover the backlog
- [ ] The GitHub Actions pipeline described in CLAUDE.md still needs to be built, or CLAUDE.md corrected to describe the manual deploy actually in use
# Under deployment requirements:
# - Run php artisan db:seed --class=AgreementTemplateSeeder (idempotent) — seeds the 3 SAMPLE platform agreement-terms templates; replace the wording with lawyer-reviewed text via Super Admin → Agreement terms before any real agreement is signed (Session 33)
# - Set RECAPTCHA_SITE_KEY + RECAPTCHA_SECRET_KEY (Google reCAPTCHA v2 checkbox) for the public lead forms — without them public leads are accepted but flagged Unverified (Session 31)
# - Run php artisan storage:link on every fresh deploy — creates public/storage → storage/app/public so QR codes + CMS images are web-served from /storage/ (idempotent; safe to re-run)
# - Run php artisan storage:check after deploy — verifies the active disk is writable and (for local) that the public/storage symlink exists; reports the active driver
# - Run php artisan db:seed --class=TenantRolesSeeder before first registration
# - Run php artisan db:seed --class=MechanicRolesSeeder before first mechanic login (needed for ROLE_SENIOR / hasRole checks)
# - Run php artisan db:seed --class=CmsContentSeeder before first landing-page load (seeds the default marketing copy; idempotent — safe to re-run after deploys)
# - Run php artisan vehicles:regenerate-qr-codes after deploying the QR-signing change (Session 21) so existing vehicles' stored QR images encode the new SIGNED scan URL; old unsigned stickers gracefully show the "expired" page until re-printed
# - [SUPERSEDED by Session 24] The S3 public-read bucket policy for cms/images/* is NO LONGER required: QR codes + CMS images now live PERMANENTLY on the local public disk (served from /storage/), even when S3 backs sensitive documents. Only re-introduce a public-read bucket policy if a future decision moves public assets onto S3